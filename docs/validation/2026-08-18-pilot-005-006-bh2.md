# 2026-08-18 Pilot 005 / 006 B/H2 验证

Status: completed; H2 failed adoption cost gate
Scope: two new high-risk B/H2 tasks, independent task Oracle, CLI JSONL access proof, sealed raw evidence

## 验收结论

- Pilot 005：4/4 为装置失败，原始分类保持 `contaminated`，不进入策略比较。
- Pilot 006：B 与 H2 的候选实现、协议、正式验证和独立任务 Oracle 均为 2/2 通过。
- Pilot 006 的冻结访问结果原为 2/4 有效；两个 PO-CR-026 run 受到 CRLF→CRCRLF stdout 翻译假阴性影响。v3 只读复核后为 4/4 有效，且四份封存 manifest 始终有效。
- H2 的总 input token 比 B 高 18.5%，没有通过预先冻结的成本门，因此不采纳。

完整指标与装置失败说明见 [结果报告](../../experiments/context-routing/results/2026-08-18-pilot-005-006-bh2-terra-medium.md)。

## 环境

- Windows 11 Professional x64
- Codex CLI 0.147.0
- `gpt-5.6-terra`, reasoning effort `medium`
- baseline commit `20fc95be7b9616fa2de90fc1ffe33b35d5c3f3fd`
- 原始根 `D:\coding warehouse\project-orrery-benchmark`

## 封存完整性

对 Pilot 006 四个运行分别执行：

```powershell
python -X utf8 experiments/context-routing/harness/seal_raw_evidence.py verify `
  --manifest <run>\raw-evidence-manifest.json
```

结果：全部 `valid: true`，文件数依次为 211、211、213、213。没有向已封存运行写入派生结果。

## v3 只读访问复核

```powershell
python -X utf8 experiments/context-routing/harness/validate_cli_events.py `
  --events <run>\_operator\events.jsonl `
  --proxy-log <run>\repository\.benchmark\proxy-audit.jsonl `
  --policy <run>\_operator\access-policy.json
```

四个运行均得到：

- `apparatus_valid: true`；
- 内容读取分别证明 1/1、2/2、3/3、3/3；
- `unapproved_commands: []`；
- `failed_proxy_commands: []`；
- `unexpected_item_types: []`；
- `unexpected_write_paths: []`；
- `missing_output_proofs: []`；
- `invalid_output_proofs: []`。

复核 validator 与 proof extractor 的 SHA-256 分别为：

- `f4d4abd924efa06b337c308b70012a7daa7117ccefa9f3eb92a151b95971d4e9`
- `d4b23b5d79c2b6587b114cb770f068a7437fa8ad40acc759d48e66ea0cb091f5`

## CRLF 安全回归

读取代理现在直接写 UTF-8 bytes，避免 Windows `TextIO` 二次翻译 CRLF。proof extractor 兼容 raw CRLF、LF 规范形式和旧运行中可逆的 CRCRLF 翻译，但每种形式都必须命中代理日志独立记录的同一个 SHA-256；篡改正文仍被拒绝。

专项命令：

```powershell
python -m unittest tests.test_context_routing_h2 -q
```

结果：11/11 通过。

## 全仓回归

```powershell
python -m unittest discover -s tests -q
$env:ORRERY_TEST_BUILD='1'; python -m unittest discover -s tests -q
python experiments/context-routing/validate_benchmark.py --repo-root .
python skills/project-orrery/scripts/validate_installation.py --target . --require-integrated --build
python -X utf8 scripts/docsite/build_docsite.py
git diff --check
```

结果：默认 39 项中 38 通过、动态 reader 按设计跳过；启用动态 reader 后 39/39 通过。24 项 corpus 与 6 份既有 run record 有效；integrated scaffold + static build、文档站、本地 Markdown 链接和 diff 检查全部通过。

## 决策边界

本验证支持保留研究 Harness，不支持把 H2 写入发布 Skill。原始运行的历史分类没有被改写；v3 是明确标注版本和哈希的只读派生复核。
