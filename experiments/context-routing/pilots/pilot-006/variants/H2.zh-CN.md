# Variant H2：Harness 生成审计结构

不要输出 Context Manifest、Selected Evidence、Scope Expansion 说明或 Access Summary。

直接使用读取代理工作。需要扩张时，只在代理 `read` 命令参数中填写一个短 reason code；Harness 会从 Prompt 配置、代理日志和完整 CLI JSONL 生成审计结构。最终回复只说明实现结果和快速反馈。
