# Variant B：模型撰写的协议基线

在第一次正文读取前，先在回复中输出完整 `CONTEXT MANIFEST`，至少包含任务分类、初始读取路径与理由、预期写入、快速验证和扩张条件。

读取第三个不同文件或扩大范围前，除了在代理命令中填写 reason code，还要先输出一段 `SCOPE EXPANSION`，说明路径、理由码和证据缺口。

最终回复附上 `ACCESS SUMMARY`：逐项列出读取路径、扩张和执行的反馈命令。不要创建回执文件。
