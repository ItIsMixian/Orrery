# PO-CR-033：自动过期不能冒充用户 snooze

修复反馈状态行为：到期反馈应进入 `expired`，但自动过期不得新建或延长 `snoozed_until`。
只有用户显式 snooze 才能进入 `snoozed` 并在截止前抑制提示；截止后恢复为 `pending`。

重复执行自动过期也不得改变既有截止时间；处于用户 snooze 中的反馈不能被自动过期抢占。

增加能同时捕获“新建冷却”和“延长冷却”缺陷的自动化测试，并按仓库规则把受影响的当前 State 与
PROGRESS 更新为实现事实。
不要修改历史 ADR、存储模块、HANDOFF 或无关文件。
