# C1 — Oracle v0.2 无模型静态 Controls

Date: 2026-08-22
Status: static controls passed; ready to request Pilot 010 design, not ready to run it
Development task: `C1` (not an R0/R1/R2 evidence-layer label)

## 结论

Oracle v0.2 的静态控制面已通过。7 文件 checksummed fixture 的四项公开测试与 20 个 cases 覆盖
baseline negative、三组 paraphrase、每项事实两组
contradiction、索引改名、关键 behavior/data/scope/State/formal mutations、未知措辞人工复核和 apparatus
contamination。所有运行只使用临时 Git 仓库、Python 公共调用链与 SQLite；`model_calls` 为 0，未创建
Pilot 010。

静态 readiness 是 **YES — 可以申请 Pilot 010 的任务与实验设计**。运行 readiness 是 **NO**：没有
Pilot 010 目录、Prompt、模型配置或 raw root，也尚未针对未来任务冻结 P/S Prompt 等长、嵌套 Git、完整
事件 validator、Scope analyzer pipeline 和目标 runtime 无模型握手。

## 分层结果

| 层 | 静态结论 | 控制证据 |
|---|---|---|
| `formal_validity` | PASS | 公开 fixture 路径、UTF-8 Python、结构化 State shape 与 task identity；缺字段 control 定位为 formal fail |
| `semantic_quality` | PASS | 公共 Feedback/SQLite 调用链、数据保留与幂等、Git 范围、三语体事实单元；关键 mutations 定位到独立维度 |
| `state_future_version` | PASS | versioned public State enums 与 `reject_before_write`；State omission 和写后拒绝分别失败 |
| `apparatus_contamination` | PASS | 外部读取 control 得到 `contaminated`，同时语义与 State 层仍保持 pass |

未知自然语言没有被误判为实现失败，而是 `manual_review_required`。SQLite 复合索引只按列顺序验收；
改名 control 仍 pass，交换列顺序 control fail。

## 可复现入口

```powershell
python -X utf8 experiments/context-routing/oracles/oracle-v0.2/oracle.py --verify-fixture
python -X utf8 experiments/context-routing/oracles/oracle-v0.2/oracle.py --self-test
python -X utf8 -m unittest tests.test_context_routing_oracle_v02 -v
```

完整仓库与文档验证见 [Validation](../../../docs/validation/2026-08-22-c1-context-routing-oracle-v0.2-static-controls.md)。

## 不可越界解释

- 本结果不重分类 Pilot 004–009，不改写冻结 Oracle 或仓库外 raw manifests。
- 静态 controls 不证明任何模型会正确完成任务，也不支持采纳 S 或其他发布策略。
- 公开 State schema 属于研究 fixture，不是 Project Orrery 发布 Skill 的新契约。
- 没有新 ADR、模型调用、真实用户项目运行、push、merge、tag 或 Release。
