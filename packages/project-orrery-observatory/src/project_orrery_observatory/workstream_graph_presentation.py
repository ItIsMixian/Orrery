"""Chinese-first progressive presentation assets for the read-only Workstream graph."""

WORKSTREAM_GRAPH_CSS = r"""
body:has(#workstream-relation-graph.on) .toc{display:none}body:has(#workstream-relation-graph.on) main.content{min-width:0}#workstream-relation-graph.page.wide.on{width:100%;max-width:none}
.wg-shell{--wg-cyan:#6ee7dd;--wg-yellow:#f4c96b;--wg-red:#ff786c;--wg-ink:var(--fg);--wg-dim:var(--mut);--wg-node-width:224px;--wg-node-height:96px;position:relative;width:100%;margin:0 0 24px;border:1px solid var(--line);border-radius:15px;overflow:hidden;background:linear-gradient(180deg,rgba(110,231,221,.045),transparent 230px),var(--bg);color:var(--fg)}
.wg-shell *{box-sizing:border-box}.wg-mast{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:18px;padding:22px 24px 17px;border-bottom:1px solid var(--line)}.wg-kicker,.wg-panel-index{margin:0;color:var(--acc);font:700 10px/1.3 "Cascadia Code",Consolas,monospace;letter-spacing:.12em}.wg-mast h2{margin:4px 0;color:var(--fg);font-size:26px;letter-spacing:-.025em}.wg-mast p{max-width:760px;margin:0;color:var(--mut);font-size:12.5px}.wg-seal{align-self:start;padding:7px 10px;border:1px solid var(--line);border-radius:6px;color:var(--wg-dim);font:700 10px/1.2 "Cascadia Code",Consolas,monospace}.wg-seal.ready::before{content:"◇ ";color:var(--wg-cyan)}.wg-seal.unavailable::before{content:"? ";color:var(--wg-yellow)}
.wg-failure{margin:16px 24px 0;padding:12px 14px;border:1px dashed var(--wg-yellow);background:rgba(244,201,107,.06);color:var(--mut);border-radius:8px}.wg-failure b{display:block;color:var(--wg-yellow);margin-bottom:3px}.wg-controls{padding:16px 24px;border-bottom:1px solid var(--line)}.wg-lenses{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:1px;background:var(--line);border:1px solid var(--line);border-radius:9px;overflow:hidden}.wg-lens{min-width:0;border:0;border-radius:0;padding:11px 13px;background:var(--bg2);color:var(--fg);font:inherit;text-align:left;cursor:pointer}.wg-lens span{display:block;color:var(--mut);font:700 9px/1.2 "Cascadia Code",Consolas,monospace}.wg-lens b{display:block;margin-top:3px}.wg-lens[aria-pressed="true"]{box-shadow:inset 0 -3px 0 var(--wg-cyan);background:rgba(110,231,221,.08)}
.wg-graph-modes{display:inline-grid;grid-template-columns:repeat(2,auto);grid-column:1/-1;justify-self:start;align-self:end;border:1px solid var(--line);border-radius:7px;overflow:hidden}.wg-mode{min-height:38px;padding:0 14px;border:0;border-right:1px solid var(--line);background:var(--bg2);color:var(--mut);font:700 11px/1.2 inherit;cursor:pointer;white-space:nowrap}.wg-mode:last-child{border-right:0}.wg-mode[aria-pressed="true"]{background:rgba(110,231,221,.1);color:var(--wg-cyan);box-shadow:inset 0 -2px 0 var(--wg-cyan)}
.wg-filterbar{display:grid;grid-template-columns:minmax(145px,.8fr) minmax(145px,1fr) minmax(145px,1fr) repeat(4,auto);gap:8px;margin-top:10px}.wg-filterbar label{display:grid;gap:4px;color:var(--mut);font-size:10px}.wg-filterbar select,.wg-button{min-height:38px;border:1px solid var(--line);border-radius:7px;background:var(--bg2);color:var(--fg);font:600 11px/1.2 inherit;padding:0 10px}.wg-button{align-self:end;cursor:pointer;white-space:nowrap}.wg-button:hover{border-color:var(--wg-cyan)}.wg-button:disabled{opacity:.45;cursor:not-allowed}.wg-context-menu{position:relative;align-self:end}.wg-context-menu>summary{display:flex;align-items:center;min-height:38px;padding:0 10px;border:1px solid var(--line);border-radius:7px;background:var(--bg2);color:var(--fg);font:600 11px/1.2 inherit;cursor:pointer;white-space:nowrap;list-style:none}.wg-context-menu>summary::-webkit-details-marker{display:none}.wg-context-menu>summary::after{content:'▾';margin-left:8px;color:var(--mut)}.wg-context-menu[open]>summary{border-color:var(--wg-cyan)}.wg-context-menu>div{position:absolute;z-index:30;right:0;top:calc(100% + 6px);display:grid;gap:8px;min-width:250px;padding:11px;border:1px solid var(--line);border-radius:8px;background:var(--bg);box-shadow:0 14px 36px rgba(0,0,0,.28)}.wg-context-option{display:flex!important;grid-template-columns:none!important;align-items:center;gap:9px!important;min-height:34px;color:var(--fg)!important;font-size:11px!important;cursor:pointer}.wg-context-option input{width:16px;height:16px;accent-color:var(--wg-cyan)}.wg-viewbar{display:flex;align-items:center;gap:7px;margin-top:9px;flex-wrap:wrap}.wg-viewbar .wg-button{min-width:38px}.wg-viewbar-note{margin-right:auto;color:var(--mut);font-size:10.5px}.wg-zoom-readout{min-width:46px;text-align:center;color:var(--wg-dim);font:700 10px/1.2 "Cascadia Code",Consolas,monospace}.wg-engine-help{margin-top:9px;border-top:1px solid var(--line);color:var(--mut);font-size:10px}.wg-engine-help>summary{width:max-content;padding:9px 0 2px;cursor:pointer}.wg-engine-help>div{display:flex;align-items:end;gap:10px;padding:7px 0}.wg-engine-help label{display:grid;gap:4px}.wg-engine-help select{min-height:34px;border:1px solid var(--line);border-radius:7px;background:var(--bg2);color:var(--fg);padding:0 9px}.wg-legacy-banner{margin-top:9px;padding:8px 10px;border:1px dashed var(--wg-yellow);border-radius:7px;color:var(--wg-yellow);font:700 10px/1.3 "Cascadia Code",Consolas,monospace}.wg-layout-loading,.wg-layout-failure{position:absolute;z-index:12;inset:18px;display:grid;align-content:center;justify-items:center;gap:9px;padding:22px;border:1px dashed var(--wg-yellow);border-radius:9px;background:color-mix(in srgb,var(--bg) 94%,transparent);text-align:center}.wg-layout-loading[hidden],.wg-layout-failure[hidden]{display:none}.wg-layout-loading>b{color:var(--wg-cyan);font-size:17px}.wg-layout-failure>b{color:var(--wg-yellow);font-size:17px}.wg-layout-loading>span,.wg-layout-failure>span{max-width:520px;color:var(--mut);font-size:11px}.wg-layout-failure>div{display:flex;gap:8px;flex-wrap:wrap;justify-content:center}
.wg-grid{min-height:560px}.wg-graph-panel{min-width:0;width:100%;padding:18px 20px 20px}.wg-panel-head{display:flex;justify-content:space-between;gap:14px;align-items:end}.wg-panel-head h3{margin:3px 0 0;color:var(--fg);font-size:17px}.wg-legend{display:flex;gap:12px;flex-wrap:wrap;color:var(--mut);font-size:10px}.wg-legend i{display:inline-block;width:24px;border-top:3px solid var(--wg-cyan);vertical-align:middle;margin-right:5px}.wg-legend .series{border-top:2px solid var(--wg-dim)}.wg-legend .dependency{border-top-style:dashed;border-color:var(--wg-yellow)}.wg-legend .conflict{border-top-style:double;border-top-width:5px;border-color:var(--wg-red)}
.wg-frame{position:relative;margin-top:12px;height:480px;border:1px solid var(--line);border-radius:9px;overflow:hidden;background:var(--bg2)}.wg-viewport{width:100%;height:100%;overflow:auto;overscroll-behavior:contain;cursor:grab;background-image:linear-gradient(rgba(148,163,183,.065) 1px,transparent 1px),linear-gradient(90deg,rgba(148,163,183,.065) 1px,transparent 1px);background-size:28px 28px}.wg-viewport.is-panning{cursor:grabbing;user-select:none}.wg-canvas{position:relative;min-width:100%;min-height:100%;padding:0}.wg-frame svg{display:block;max-width:none;overflow:visible}.wg-lane{fill:rgba(148,163,183,.035);stroke:rgba(148,163,183,.16);stroke-width:1}.wg-lane-title{fill:var(--wg-dim);font:700 11px "Cascadia Code",Consolas,monospace;letter-spacing:.06em}.wg-lane-index{fill:var(--wg-cyan);font:700 9px "Cascadia Code",Consolas,monospace}.wg-series-band{fill:rgba(110,231,221,.028);stroke:rgba(110,231,221,.23);stroke-width:1;stroke-dasharray:3 5}.wg-series-title{fill:var(--wg-cyan);font:700 10px "Cascadia Code",Consolas,monospace;letter-spacing:.07em}.wg-program-band{fill:rgba(110,231,221,.018);stroke:rgba(110,231,221,.24);stroke-width:1.5}.wg-phase-band{fill:rgba(148,163,183,.026);stroke:rgba(148,163,183,.22);stroke-width:1;stroke-dasharray:5 5}.wg-program-title{fill:var(--wg-cyan);font:800 11px "Microsoft YaHei UI","PingFang SC",sans-serif}.wg-phase-title{fill:var(--wg-dim);font:700 10px "Microsoft YaHei UI","PingFang SC",sans-serif}
.wg-program-band,.wg-program-title{display:none}
.wg-edge{fill:none;stroke-width:3;stroke-linejoin:round;stroke-linecap:square;vector-effect:non-scaling-stroke;transition:opacity .15s}.wg-edge.lens-succession{stroke:var(--wg-cyan)}.wg-edge.lens-dependency{stroke:var(--wg-yellow);stroke-dasharray:12 8}.wg-edge.lens-conflict{stroke:var(--wg-red);stroke-width:4;stroke-dasharray:18 5 3 5}.wg-edge.series-display{stroke:var(--wg-dim);stroke-width:1.5}.wg-edge.comparison-suggestion{stroke:var(--wg-yellow);stroke-width:1.5;stroke-dasharray:1 8;opacity:.68}.wg-bundle-trunk{fill:none;stroke:var(--wg-cyan);stroke-width:3;vector-effect:non-scaling-stroke}.wg-bundle-trunk.lens-dependency{stroke:var(--wg-yellow);stroke-dasharray:12 8}.wg-bundle-trunk.lens-conflict{stroke:var(--wg-red)}.wg-bundle-hit{fill:none;stroke:transparent;stroke-width:22;pointer-events:stroke;cursor:pointer}.wg-edge.proposed{opacity:.78}.wg-edge.stale{opacity:.56}.wg-edge.uncertain{opacity:.72}.wg-edge.is-muted,.wg-node.is-muted{opacity:.16}.wg-edge.is-selected{opacity:1;stroke-width:5}.wg-edge-hit{fill:none;stroke:transparent;stroke-width:20;pointer-events:stroke;cursor:pointer;outline:none}.wg-edge-hit:focus,.wg-edge-hit:focus-visible{outline:none}.wg-edge-hit:focus-visible{stroke:var(--wg-cyan);stroke-width:10;stroke-dasharray:2 7;opacity:.38;vector-effect:non-scaling-stroke}.wg-edge-label{fill:var(--wg-ink);font:700 10px "Microsoft YaHei UI","PingFang SC",sans-serif;paint-order:stroke;stroke:var(--bg2);stroke-width:7px;stroke-linejoin:round;pointer-events:none}.wg-edge-label.series-display{fill:var(--wg-dim);font-weight:800}.wg-edge-label.comparison-suggestion{fill:var(--wg-yellow)}
.wg-node{--wg-node-accent:var(--line);cursor:pointer;outline:none!important}.wg-node>rect{fill:var(--bg);stroke:var(--wg-node-accent);stroke-width:2}.wg-node.current{--wg-node-accent:var(--wg-cyan)}.wg-node.current>rect{stroke-width:3}.wg-node.direct{--wg-node-accent:#aebbd0}.wg-node.history,.wg-node.cluster,.wg-node.boundary{--wg-node-accent:var(--wg-dim)}.wg-node.history>rect{stroke-dasharray:5 4}.wg-node.boundary>rect{fill:rgba(148,163,183,.025);stroke-dasharray:2 6}.wg-node.unknown{--wg-node-accent:var(--wg-yellow)}.wg-node.unknown>rect{stroke-dasharray:7 5}.wg-node.conflict{--wg-node-accent:var(--wg-red)}.wg-node.conflict>rect{stroke-width:3}.wg-node.cluster>rect{fill:rgba(110,231,221,.045);stroke-dasharray:8 5}.wg-node.cluster:hover>rect{filter:brightness(1.18)}.wg-node.is-endpoint>rect,.wg-node.is-node-selected>rect{stroke:var(--wg-node-accent);stroke-width:3;filter:brightness(1.16)}.wg-node-role{font:700 10px "Cascadia Code",Consolas,monospace}.wg-node.current .wg-node-role{fill:var(--wg-cyan)}.wg-node.direct .wg-node-role,.wg-node.history .wg-node-role,.wg-node.cluster .wg-node-role,.wg-node.boundary .wg-node-role{fill:var(--wg-dim)}.wg-node.unknown .wg-node-role{fill:var(--wg-yellow)}.wg-node.conflict .wg-node-role{fill:var(--wg-red)}.wg-node-prefix{fill:var(--fg);font:800 15px "Cascadia Code",Consolas,monospace}.wg-node-name{fill:var(--wg-ink);font:700 13px/1.2 "Microsoft YaHei UI","PingFang SC",sans-serif}.wg-node-id{fill:var(--wg-dim);font:10px "Cascadia Code",Consolas,monospace}.wg-node-meta{fill:var(--wg-dim);font:10px "Microsoft YaHei UI","PingFang SC",sans-serif}.wg-chain-collapse rect{fill:var(--bg2);stroke:var(--wg-cyan);stroke-width:1}.wg-chain-collapse text{fill:var(--wg-cyan);font:700 9px "Microsoft YaHei UI","PingFang SC",sans-serif}.wg-chain-collapse:focus-visible{outline:none}.wg-chain-collapse:focus-visible rect{stroke:var(--wg-yellow);stroke-width:3}.wg-empty{position:absolute;inset:0;display:grid;place-content:center;padding:36px;text-align:center;color:var(--mut);background:var(--bg2);pointer-events:none}.wg-empty b{color:var(--fg);font-size:18px}.wg-empty p{max-width:520px;margin:9px auto 0;font-size:12px;line-height:1.65}.wg-empty small{display:block;margin-top:10px;color:var(--wg-dim);font:700 10px/1.4 "Cascadia Code",Consolas,monospace}.wg-empty[hidden]{display:none}
.wg-node.cluster.history-fold>rect{fill:rgba(110,231,221,.075);stroke:var(--wg-cyan)}
.wg-series-guide{stroke:var(--line);stroke-width:1;opacity:.62;vector-effect:non-scaling-stroke}.wg-total-succession .wg-series-title{fill:var(--wg-dim);font-size:9px;letter-spacing:.08em}.wg-total-succession .wg-edge.lens-succession.semantic{stroke-width:2.1;opacity:.74}.wg-total-succession .wg-edge.lens-succession.semantic.proposed{opacity:.7}.wg-total-succession .wg-edge.lens-succession.semantic.stale{opacity:.68}.wg-total-succession .wg-edge.series-display{stroke-width:1.1;opacity:.44}
.wg-inspector[hidden]{display:none!important}.wg-inspector{position:absolute;z-index:20;top:12px;right:12px;bottom:12px;width:min(390px,calc(100% - 24px));overflow:auto;padding:18px;border:1px solid var(--line);border-radius:10px;background:var(--bg);box-shadow:-10px 0 35px rgba(0,0,0,.32)}.wg-inspector-head{display:flex;align-items:center;justify-content:space-between;gap:12px;border-bottom:1px solid var(--line);padding-bottom:10px;color:var(--mut);font:700 9px/1.2 "Cascadia Code",Consolas,monospace;letter-spacing:.08em}.wg-inspector-close{width:34px;height:34px;border:1px solid var(--line);border-radius:7px;background:var(--bg2);color:var(--fg);font-size:20px;cursor:pointer}.wg-inspector h3{margin:15px 0 4px;font-size:18px;overflow-wrap:anywhere}.wg-inspector-summary{margin:0 0 12px;color:var(--mut);font-size:12px}.wg-axis{display:grid;grid-template-columns:minmax(90px,.75fr) minmax(0,1.25fr);gap:8px;padding:7px 0;border-top:1px solid var(--line);font-size:11px}.wg-axis span{color:var(--mut)}.wg-axis b{overflow-wrap:anywhere;text-align:right}.wg-reasons{margin:8px 0;padding-left:17px;color:var(--wg-yellow);font:10px/1.6 "Cascadia Code",Consolas,monospace}.wg-relation-history{display:grid;gap:7px;margin-top:12px;padding-top:11px;border-top:1px solid var(--line)}.wg-relation-history h4{margin:0;color:var(--wg-yellow);font:700 10px/1.3 "Cascadia Code",Consolas,monospace}.wg-relation-revision{display:grid;gap:3px;padding:8px;border:1px solid var(--line);border-radius:6px;background:var(--bg2);color:var(--mut);font:10px/1.4 "Cascadia Code",Consolas,monospace;overflow-wrap:anywhere}.wg-relation-revision b{color:var(--fg)}.wg-relation-revision small{color:var(--mut)}.wg-evidence{display:grid;gap:6px;margin-top:12px}.wg-evidence a,.wg-evidence span{display:block;max-width:100%;padding:7px 8px;border:1px solid var(--line);border-radius:6px;color:var(--mut);font:10px/1.35 "Cascadia Code",Consolas,monospace;overflow-wrap:anywhere}.wg-evidence a{color:var(--wg-cyan);text-decoration:none}.wg-readonly{margin-top:14px;padding-top:10px;border-top:1px solid var(--line);color:var(--mut);font-size:10px}
.wg-mobile-ledger{position:absolute;width:1px;height:1px;padding:0;margin:-1px;overflow:hidden;clip-path:inset(50%);white-space:nowrap;border:0}.wg-mobile-ledger>summary{cursor:pointer}.wg-ledger{padding:14px}.wg-ledger h3{margin:0 0 4px}.wg-ledger-intro{margin:0 0 12px;color:var(--mut);font-size:11px}.wg-ledger-rank{margin:12px 0 7px;color:var(--wg-cyan);font:700 10px "Cascadia Code",Consolas,monospace}.wg-ledger-list{display:grid;gap:8px}.wg-ledger-item{width:calc(100% - var(--wg-indent,0px));margin-left:var(--wg-indent,0px);min-width:0;padding:11px;border:1px solid var(--line);border-left:4px solid var(--wg-cyan);border-radius:7px;background:var(--bg2);color:var(--fg);font:inherit;text-align:left}.wg-ledger-item.direct{border-left-color:#aebbd0}.wg-ledger-item.history,.wg-ledger-item.cluster{border-left-color:var(--wg-dim);border-left-style:dashed}.wg-ledger-item.unknown{border-left-color:var(--wg-yellow);border-left-style:dashed}.wg-ledger-item.conflict{border-left-color:var(--wg-red)}.wg-ledger-item b,.wg-ledger-item small{display:block;overflow-wrap:anywhere}.wg-ledger-item small{margin-top:4px;color:var(--mut)}.wg-ledger-collapse{width:auto;margin-left:auto;padding:7px 10px;border:1px solid var(--wg-cyan);border-radius:6px;background:var(--bg2);color:var(--wg-cyan);font:700 10px inherit}.wg-ledger-edge{padding:9px 11px;border:1px solid var(--line);border-radius:7px;background:rgba(148,163,183,.035);color:var(--fg);text-align:left}.wg-ledger-edge b{color:var(--wg-cyan)}.wg-ledger-edge.dependency b{color:var(--wg-yellow)}.wg-ledger-edge.conflict b{color:var(--wg-red)}.wg-ledger-edge small{display:block;margin-top:4px;color:var(--mut);overflow-wrap:anywhere}.wg-ledger-empty{padding:14px;border:1px dashed var(--line);color:var(--mut)}
.wg-comparison-drawer{border-top:1px solid var(--line);padding:0 20px}.wg-comparison-drawer>summary{display:flex;align-items:center;gap:10px;min-height:48px;cursor:pointer;color:var(--mut);font-size:11px}.wg-comparison-drawer>summary::marker{color:var(--wg-yellow)}.wg-comparison-drawer>summary b{margin-left:auto;min-width:28px;padding:3px 7px;border:1px solid var(--line);border-radius:999px;color:var(--wg-yellow);text-align:center}.wg-comparison-drawer>p{margin:0 0 10px;color:var(--mut);font-size:10.5px}.wg-comparison-drawer [data-wg-comparisons]{display:grid;gap:6px;padding-bottom:14px}.wg-comparison{width:100%;padding:9px 10px;border:1px dashed var(--wg-yellow);border-radius:7px;background:rgba(244,201,107,.045);color:var(--fg);text-align:left}.wg-comparison b,.wg-comparison small{display:block;overflow-wrap:anywhere}.wg-comparison b{color:var(--wg-yellow);font-size:11px}.wg-comparison small{margin-top:4px;color:var(--mut);font-size:10px}
svg g.is-muted{opacity:.22;transition:opacity .18s ease}.wg-node.is-muted{opacity:.38}svg g.is-selected .wg-edge{opacity:1;stroke-width:4;filter:drop-shadow(0 0 4px rgba(110,231,221,.34))}svg g.is-selected .wg-edge-label{font-weight:800}.wg-lens:focus-visible,.wg-button:focus-visible,.wg-filterbar select:focus-visible,.wg-ledger-item:focus-visible,.wg-ledger-edge:focus-visible,.wg-inspector-close:focus-visible,.wg-evidence a:focus-visible,.wg-comparison:focus-visible,.wg-mobile-ledger>summary:focus-visible,.wg-comparison-drawer>summary:focus-visible{outline:3px solid var(--wg-yellow);outline-offset:3px}.wg-node:focus,.wg-node:focus-visible{outline:none!important}.wg-node:focus>rect,.wg-node:focus-visible>rect{stroke:var(--wg-node-accent);stroke-width:3;filter:brightness(1.16)}.wg-sr{position:absolute!important;width:1px;height:1px;padding:0;margin:-1px;overflow:hidden;clip:rect(0,0,0,0);white-space:nowrap;border:0}
.wg-total-succession svg g.is-selected .wg-edge.lens-succession.semantic{stroke-width:4;opacity:1;filter:drop-shadow(0 0 4px rgba(110,231,221,.34))}.wg-total-succession svg g.is-selected .wg-edge.series-display{stroke-width:2;opacity:1}
@media(max-width:900px){.wg-filterbar{grid-template-columns:repeat(2,minmax(0,1fr)) repeat(3,auto)}.wg-frame{height:430px}}
@media(max-width:640px){header.top{gap:8px;padding:0 12px}.top h1{min-width:0;overflow:hidden;text-overflow:ellipsis}.top .sub,.searchwrap{display:none}.rightgrp{min-width:0;gap:6px}.wg-mast{grid-template-columns:1fr;padding:18px}.wg-seal{justify-self:start}.wg-controls{padding:14px 18px}.wg-lenses{grid-template-columns:1fr}.wg-filterbar{grid-template-columns:minmax(0,1fr)}.wg-filterbar label,.wg-filterbar select,.wg-button{min-width:0;width:100%}.wg-graph-modes{grid-column:1/-1;width:100%}.wg-mode{min-width:0}.wg-context-menu{width:100%}.wg-context-menu>summary{width:100%;justify-content:space-between}.wg-context-menu>div{position:static;min-width:0;margin-top:6px;box-shadow:none}.wg-viewbar{display:none}.wg-engine-help>div{display:grid}.wg-grid{min-height:0}.wg-graph-panel{display:block;padding:14px 10px}.wg-panel-head{align-items:start}.wg-legend{display:none}.wg-frame{height:500px}.wg-node-name{font-size:13px}.wg-node-role,.wg-node-meta{font-size:12px}.wg-mobile-ledger{position:static;width:auto;height:auto;padding:0;margin:0;overflow:visible;clip-path:none;white-space:normal;border-top:1px solid var(--line)}.wg-mobile-ledger>summary{padding:14px 18px;color:var(--wg-cyan);font:700 12px "Microsoft YaHei UI","PingFang SC",sans-serif}.wg-inspector{position:fixed;top:auto;left:8px;right:8px;bottom:8px;width:auto;max-height:78vh;border-radius:12px}.wg-shell{max-width:100%;overflow:hidden}.wg-comparison-drawer{padding:0 18px}}
@media(prefers-reduced-motion:reduce){.wg-shell *{scroll-behavior:auto!important;transition:none!important;animation:none!important}}
"""


WORKSTREAM_GRAPH_JS = r"""
(()=>{'use strict';
const page=document.querySelector('#workstream-relation-graph');if(!page)return;
const data=JSON.parse(page.querySelector('[data-wg-payload]').textContent);
const svg=page.querySelector('[data-wg-svg]'),viewport=page.querySelector('[data-wg-viewport]'),ledger=page.querySelector('[data-wg-ledger]');
const inspector=page.querySelector('[data-wg-inspector-shell]'),inspectorBody=page.querySelector('[data-wg-inspector]'),live=page.querySelector('[data-wg-live]');
const comparisons=page.querySelector('[data-wg-comparisons]'),comparisonCount=page.querySelector('[data-wg-comparison-count]'),comparisonToggle=page.querySelector('[data-wg-comparison-toggle]');
const scope=page.querySelector('[data-wg-scope]'),subsystem=page.querySelector('[data-wg-subsystem]'),runtime=page.querySelector('[data-wg-runtime]'),semanticContext=page.querySelector('[data-wg-semantic-context]'),affectedContext=page.querySelector('[data-wg-affected-context]'),zoomReadout=page.querySelector('[data-wg-zoom-readout]');
const engineSelect=page.querySelector('[data-wg-engine]'),engineStatus=page.querySelector('[data-wg-engine-status]'),classificationStatus=page.querySelector('[data-wg-classification-status]'),legacyBanner=page.querySelector('[data-wg-legacy-banner]'),layoutLoading=page.querySelector('[data-wg-layout-loading]'),layoutFailure=page.querySelector('[data-wg-layout-failure]'),layoutError=page.querySelector('[data-wg-layout-error]');
const state={lens:'succession',graphMode:'full',expandedChains:new Set(),expandedFolds:new Set(),scope:'all',subsystem:'all',runtime:'all',selection:null,zoom:1,comparisons:false,externalContext:false,affectedContext:false,engine:'elk',viewportAnchor:null};
const NS='http://www.w3.org/2000/svg',NODE_W=224,NODE_H=96,RANK_GAP=144,ROW_GAP=40,SERIES_GAP=88,SERIES_ROW_GAP=54,COMPONENT_GAP=54,PROGRAM_GAP=120,MIN_ZOOM=.3,MAX_ZOOM=2;
const nodeMap=new Map((data.nodes||[]).map(n=>[n.workstream_id,n]));
const activeTipIds=new Set(data.active_tip_workstream_ids||[]);
const programGroups=new Map((data.program_groups||[]).map(group=>[group.group_id,group]));
const phaseLabels={created:'已创建',planning:'规划中',implementing:'实现中',validating:'验证中','review-ready':'等待审查',integrated:'已集成',closed:'已关闭',historical:'历史任务',unknown:'关系证据不足'};
const runtimeLabels={active:'进行中',paused:'已暂停','waiting-for-user':'等待人工确认','blocked-by-conflict':'存在冲突',failed:'失败',offline:'离线',collapsed:'已收起',unknown:'关系证据不足'};
const evidenceLabels={current:'当前',stale:'证据过期',unknown:'关系证据不足',historical:'历史任务'};
const subsystemLabels={'authority-meta-model':'权威模型','documentation-system':'文档系统','multi-worktree-collaboration':'多工作树协作','project-structure':'项目结构','release-and-toolchain':'发布与工具链','test-coverage':'测试覆盖',unknown:'模块待确认','历史任务':'历史任务'};
const shown=(map,value)=>map[value]||value||'关系证据不足';
const el=(tag,cls,text)=>{const n=document.createElement(tag);if(cls)n.className=cls;if(text!==undefined)n.textContent=text;return n};
const se=(tag,attrs={})=>{const n=document.createElementNS(NS,tag);Object.entries(attrs).forEach(([k,v])=>n.setAttribute(k,String(v)));return n};
const sorted=value=>[...value].sort((a,b)=>String(a).localeCompare(String(b)));
function keyboardActivate(target,action){target.addEventListener('click',action);target.addEventListener('keydown',event=>{if(event.key==='Enter'||event.key===' '){event.preventDefault();action()}})}
function relationLabel(edge,compact=false){if(edge.relation_type==='series-display')return compact?'系列演进':'同系列演进（仅展示）';if(edge.relation_type==='comparison-suggestion')return compact?'比较建议':'需要比较／证据待刷新';if(state.lens==='conflict')return edge.certainty==='confirmed'?(compact?'确认冲突':'已确认任务冲突'):(compact?'冲突待确认':'冲突事实待确认');if(edge.collapsedHistoryEdge)return'历史接续';if(edge.lifecycle==='proposed'||edge.certainty==='unknown')return state.lens==='dependency'?(compact?'依赖待确认':'依赖等待人工确认'):(compact?'接续待确认':'接续等待人工确认');if(state.lens==='succession'&&edge.relation_type==='derived_from'&&(!edge.effective_active_succession||(edge.evidence_reason_codes||[]).includes('endpoint-not-current')))return compact?'接续待刷新':'接续状态待刷新';if(edge.lifecycle==='stale')return state.lens==='dependency'?(compact?'依赖过期':'依赖证据过期'):(compact?'接续过期':'接续证据过期');return state.lens==='dependency'?'依赖':'接续'}
function seriesDisplayEdges(){const groups=new Map();(data.nodes||[]).forEach(node=>{if(!node.series_id||node.series_order==null)return;if(!groups.has(node.series_id))groups.set(node.series_id,[]);groups.get(node.series_id).push(node)});const result=[];sorted(groups.keys()).forEach(seriesId=>{const nodes=groups.get(seriesId).sort((a,b)=>a.series_order-b.series_order||a.workstream_id.localeCompare(b.workstream_id));for(let index=1;index<nodes.length;index++){const previous=nodes[index-1],current=nodes[index];result.push({displayEdgeId:`series:${seriesId}:${previous.workstream_id}:${current.workstream_id}`,relation_type:'series-display',lifecycle:'presentation-only',certainty:'explicit-metadata',series_id:seriesId,source_workstream_id:current.workstream_id,target_workstream_id:previous.workstream_id,displayFrom:previous.workstream_id,displayTo:current.workstream_id,presentation_only:true,reason_codes:['explicit-series-adjacency-presentation-only'],source_links:[]})}});return result}
function comparisonDisplayEdges(){return(data.comparison_suggestions||[]).map((edge,index)=>{const pair=[edge.source_workstream_id,edge.target_workstream_id].sort();return{...edge,displayFrom:pair[0],displayTo:pair[1],displayEdgeId:edge.id||`comparison-${index}`,relation_type:'comparison-suggestion',lifecycle:'derived-review-only',certainty:'unconfirmed',presentation_only:true}})}
function relationSemanticKey(edge){return[edge.relation_type,edge.source_workstream_id,edge.target_workstream_id,edge.required_for||'',edge.direction||edge.display_direction||'',edge.style||edge.line_style||''].join('|')}
function relationRevisionScore(edge){const lifecycle={active:500,confirmed:450,proposed:250,stale:100,superseded:50,cancelled:0}[edge.lifecycle]||0,evidence=edge.evidence_status==='current'||edge.evidence?.status==='current'?80:edge.evidence_status==='confirmed'||edge.evidence?.status==='confirmed'?60:0,effective=edge.effective_active_succession?40:0,certainty=edge.certainty==='confirmed'?20:0;return lifecycle+evidence+effective+certainty}
function aggregateRelationRevisions(edges){const groups=new Map();edges.forEach(edge=>{const key=relationSemanticKey(edge);if(!groups.has(key))groups.set(key,[]);groups.get(key).push(edge)});const heads=[];sorted(groups.keys()).forEach(key=>{const revisions=groups.get(key).sort((a,b)=>relationRevisionScore(b)-relationRevisionScore(a)||String(b.relation_id).localeCompare(String(a.relation_id))),head=revisions[0],history=revisions.slice(1).map(item=>({relation_id:item.relation_id,lifecycle:item.lifecycle,certainty:item.certainty,evidence_status:item.evidence_status,effective_active_succession:item.effective_active_succession,evidence_reason_codes:[...(item.evidence_reason_codes||[])],source_links:[...(item.source_links||[])]}));heads.push({...head,relation_revision_count:revisions.length,relation_history:history})});return heads}
function declareBundles(edges,blockById){const eligible=edge=>!['series-display','comparison-suggestion','conflict-fact'].includes(edge.relation_type)&&!['unknown','unconfirmed','proposed'].includes(edge.certainty)&&!['proposed','stale'].includes(edge.lifecycle)&&[nodeMap.get(edge.displayFrom),nodeMap.get(edge.displayTo)].every(node=>node&&node.evidence_freshness==='current'&&node.scope_status==='current');const assigned=new Set();[['source','displayFrom'],['target','displayTo']].forEach(([endpoint,key])=>{const groups=new Map();edges.filter(eligible).filter(edge=>!assigned.has(edge.displayEdgeId)).forEach(edge=>{const blockPair=[blockById.get(edge.displayFrom)||'unassigned',blockById.get(edge.displayTo)||'unassigned'].join('>'),signature=[endpoint,edge[key],edge.relation_type,edge.lifecycle,edge.certainty,edge.required_for||'',blockPair].join('|');if(!groups.has(signature))groups.set(signature,[]);groups.get(signature).push(edge)});sorted(groups.keys()).forEach(signature=>{const values=groups.get(signature);if(values.length<2)return;const bundleId=`bundle:${signature.replace(/[^A-Za-z0-9._-]/g,'-')}`;values.forEach(edge=>{edge.routeBundleId=bundleId;edge.routeBundleEndpoint=endpoint;assigned.add(edge.displayEdgeId)})})});return edges}
function rawLensEdges(){
  if(state.lens==='conflict')return(data.conflicts||[]).map((edge,index)=>{const pair=[edge.source_workstream_id,edge.target_workstream_id].sort();return{...edge,displayFrom:pair[0],displayTo:pair[1],displayEdgeId:edge.id||`conflict-${index}`}});
  const allowed=state.lens==='succession'?new Set(['derived_from','absorbs']):new Set(['depends_on']);
  const semantic=aggregateRelationRevisions((data.edges||[]).filter(edge=>allowed.has(edge.relation_type)&&edge.lifecycle!=='cancelled')).map((edge,index)=>({...edge,displayFrom:edge.target_workstream_id,displayTo:edge.source_workstream_id,displayEdgeId:edge.relation_id||`edge-${index}`}));
  return[...semantic,...seriesDisplayEdges(),...(state.comparisons?comparisonDisplayEdges():[])];
}
function matches(node){const unassigned=!node.primary_subsystem_id||node.primary_subsystem_id==='unknown'||node.primary_subsystem_id==='unregistered',scopeMatch=state.scope==='all'||(state.scope==='program-w'&&node.program_id==='workstream-w'),primaryMatch=state.subsystem==='all'||(state.subsystem==='unassigned'?unassigned:node.primary_subsystem_id===state.subsystem),affectedMatch=state.affectedContext&&state.subsystem!=='all'&&(node.affected_subsystem_ids||[]).includes(state.subsystem);return scopeMatch&&(primaryMatch||affectedMatch)&&(state.runtime==='all'||node.runtime_condition===state.runtime)}
function orderedAncestors(tip,incoming){const found=new Set(),ordered=[];function visit(id){sorted(incoming.get(id)||[]).forEach(item=>{if(found.has(item))return;found.add(item);visit(item);ordered.push(item)})}visit(tip);return ordered}
function historyFoldEligible(node){if(!node||activeTipIds.has(node.workstream_id))return false;if(!['closed','completed','superseded','historical','archived'].includes(node.lifecycle_phase))return false;if(node.evidence_freshness!=='current'||node.scope_status!=='current'||!node.closure_reason)return false;if(['human-confirmation-pending','stale-evidence','relation-evidence-insufficient','missing-task-record','unregistered','unknown'].includes(node.plain_status_code))return false;return!(data.edges||[]).some(edge=>[edge.source_workstream_id,edge.target_workstream_id].includes(node.workstream_id)&&(edge.certainty!=='confirmed'||['proposed','stale'].includes(edge.lifecycle)))}
function partialOrderEdges(){const result=seriesDisplayEdges();(data.edges||[]).forEach(edge=>{if(edge.relation_type!=='derived_from'||edge.certainty!=='confirmed'||!edge.effective_active_succession||['proposed','stale','cancelled','superseded'].includes(edge.lifecycle))return;const from=nodeMap.get(edge.target_workstream_id),to=nodeMap.get(edge.source_workstream_id);if(!from||!to||[from,to].some(node=>node.evidence_freshness!=='current'||node.scope_status!=='current'))return;result.push({...edge,displayFrom:edge.target_workstream_id,displayTo:edge.source_workstream_id,displayEdgeId:edge.relation_id})});return result}
function localRanks(memberIds,rankEdges,hiddenOwner){const indegree=new Map([...memberIds].map(id=>[id,0])),adjacency=new Map([...memberIds].map(id=>[id,new Set()]));rankEdges.forEach(edge=>{const from=hiddenOwner.get(edge.displayFrom)||edge.displayFrom,to=hiddenOwner.get(edge.displayTo)||edge.displayTo;if(from===to||!memberIds.has(from)||!memberIds.has(to)||adjacency.get(from).has(to))return;adjacency.get(from).add(to);indegree.set(to,indegree.get(to)+1)});const rank=new Map([...memberIds].map(id=>[id,0])),queue=sorted([...memberIds].filter(id=>indegree.get(id)===0));while(queue.length){const id=queue.shift();sorted(adjacency.get(id)).forEach(to=>{rank.set(to,Math.max(rank.get(to),rank.get(id)+1));indegree.set(to,indegree.get(to)-1);if(indegree.get(to)===0){queue.push(to);queue.sort()}})}return rank}
const rectIntersects=(a,b)=>a.x<b.x+b.w&&a.x+a.w>b.x&&a.y<b.y+b.h&&a.y+a.h>b.y;
const rectInside=(inner,outer)=>inner.x>=outer.x&&inner.y>=outer.y&&inner.x+inner.w<=outer.x+outer.w&&inner.y+inner.h<=outer.y+outer.h;
function segmentHitsRect(start,end,rect,inset=1){const left=rect.x+inset,right=rect.x+rect.w-inset,top=rect.y+inset,bottom=rect.y+rect.h-inset;if(start[1]===end[1]){const low=Math.min(start[0],end[0]),high=Math.max(start[0],end[0]);return top<=start[1]&&start[1]<=bottom&&Math.max(low,left)<=Math.min(high,right)}if(start[0]===end[0]){const low=Math.min(start[1],end[1]),high=Math.max(start[1],end[1]);return left<=start[0]&&start[0]<=right&&Math.max(low,top)<=Math.min(high,bottom)}return true}
function placeRouteLabels(routes,positions,componentBounds,componentHeaders){const labels=[],allSegments=routes.flatMap(route=>route.points.slice(1).map((point,index)=>[route.points[index],point])),boundsByComponent=new Map(componentBounds.map(item=>[item.component,item]));routes.forEach(route=>{if(!route.hasLabel)return;const width=116,height=18,owner=boundsByComponent.get(route.component),segments=route.points.slice(1).map((point,index)=>({a:route.points[index],b:point,length:Math.hypot(point[0]-route.points[index][0],point[1]-route.points[index][1])})).sort((a,b)=>b.length-a.length),candidates=[];segments.forEach(segment=>{const midX=Math.round((segment.a[0]+segment.b[0])/2),midY=Math.round((segment.a[1]+segment.b[1])/2);if(segment.a[1]===segment.b[1])[-30,12,-54,36].forEach(offset=>candidates.push({x:midX-width/2,y:midY+offset,w:width,h:height}));else[14,-width-14,32,-width-32].forEach(offset=>candidates.push({x:midX+offset,y:midY-height/2,w:width,h:height}))});candidates.push({x:owner.x+10+(labels.length%4)*(width+8),y:owner.y+8+Math.floor(labels.length/4)*(height+4),w:width,h:height});const obstacles=[...positions.values(),...componentHeaders,...labels],candidate=candidates.find(rect=>rectInside(rect,owner)&&!obstacles.some(item=>rectIntersects(rect,item))&&!allSegments.some(([start,end])=>segmentHitsRect(start,end,rect,0)))||{...candidates.at(-1),placementFailed:true};const label={...candidate,component:route.component,edgeId:route.edge.displayEdgeId};labels.push(label);route.labelRect=label;route.labelPoint=[label.x+label.w/2,label.y+label.h]});return labels}
function verifyGeometry(model){const counts={node_overlap:0,decoration_card_overlap:0,component_overlap:0,route_card_intersection:0,route_label_intersection:0,route_crossing:0,outside_component:0,outside_canvas:0,duplicate_decoration:0,placement_translation:0},nodes=[...model.positions.entries()],boundsByComponent=new Map(model.componentBounds.map(item=>[item.component,item]));nodes.forEach(([id,left],index)=>nodes.slice(index+1).forEach(([,right])=>{if(rectIntersects(left,right))counts.node_overlap++}));model.componentHeaders.forEach(header=>nodes.forEach(([,node])=>{if(rectIntersects(header,node))counts.decoration_card_overlap++}));model.componentBounds.forEach((left,index)=>model.componentBounds.slice(index+1).forEach(right=>{if(rectIntersects(left,right))counts.component_overlap++}));nodes.forEach(([,node])=>{if(!rectInside(node,boundsByComponent.get(node.component)))counts.outside_component++});model.labelRects.forEach(label=>{if(label.placementFailed||!rectInside(label,boundsByComponent.get(label.component)))counts.outside_component++});const segments=[];model.routes.forEach(route=>{const endpoints=new Set([route.edge.displayFrom,route.edge.displayTo]),owner=boundsByComponent.get(route.component);if(route.points.some(point=>point[0]<owner.x-12||point[0]>owner.x+owner.w+12||point[1]<owner.y-12||point[1]>owner.y+owner.h+12))counts.outside_component++;route.points.slice(1).forEach((end,index)=>{const start=route.points[index];segments.push({edgeId:route.edge.displayEdgeId,start,end});nodes.forEach(([id,node])=>{if(!endpoints.has(id)&&segmentHitsRect(start,end,node))counts.route_card_intersection++});model.labelRects.forEach(label=>{if(segmentHitsRect(start,end,label,0))counts.route_label_intersection++})})});segments.forEach((left,index)=>segments.slice(index+1).forEach(right=>{if(left.edgeId===right.edgeId||[left.start,left.end].some(point=>right.start[0]===point[0]&&right.start[1]===point[1]||right.end[0]===point[0]&&right.end[1]===point[1]))return;const lh=left.start[1]===left.end[1],rh=right.start[1]===right.end[1];if(lh===rh)return;const h=lh?left:right,v=lh?right:left,hx=[h.start[0],h.end[0]].sort((a,b)=>a-b),vy=[v.start[1],v.end[1]].sort((a,b)=>a-b);if(hx[0]<v.start[0]&&v.start[0]<hx[1]&&vy[0]<h.start[1]&&h.start[1]<vy[1])counts.route_crossing++}));if(model.placementTranslations.length!==model.componentBounds.length||new Set(model.placementTranslations.map(item=>item.component)).size!==model.placementTranslations.length)counts.placement_translation++;[...nodes.map(([,item])=>item),...model.componentBounds,...model.labelRects].forEach(item=>{if(item.x<0||item.y<0||item.x+item.w>model.width||item.y+item.h>model.height)counts.outside_canvas++});return{passed:!Object.values(counts).some(Boolean),counts}}
function buildModel(graphMode=state.graphMode){
  let rawEdges=rawLensEdges();const scopedFullIds=new Set((data.nodes||[]).filter(matches).map(node=>node.workstream_id));let baseFullIds=new Set(scopedFullIds);if(state.lens==='dependency'){const dependencyEdges=rawEdges.filter(edge=>edge.relation_type==='depends_on'),dependencyEndpoints=new Set(dependencyEdges.flatMap(edge=>[edge.displayFrom,edge.displayTo]));baseFullIds=new Set([...scopedFullIds].filter(id=>dependencyEndpoints.has(id)));const admittedSeries=rawEdges.filter(edge=>edge.relation_type==='series-display'&&baseFullIds.has(edge.displayFrom)&&baseFullIds.has(edge.displayTo));rawEdges=[...dependencyEdges,...admittedSeries]}const externalIds=new Set();rawEdges.forEach(edge=>{const fromIn=baseFullIds.has(edge.displayFrom),toIn=baseFullIds.has(edge.displayTo);if(fromIn&&!toIn&&nodeMap.has(edge.displayTo))externalIds.add(edge.displayTo);if(toIn&&!fromIn&&nodeMap.has(edge.displayFrom))externalIds.add(edge.displayFrom)});const allowedNodes=new Set([...baseFullIds,...(state.externalContext?externalIds:[])]),visibleNodeMap=new Map(nodeMap),boundaryByExternal=new Map();if(!state.externalContext)externalIds.forEach(id=>{const source=nodeMap.get(id),stubId=`boundary:${id}`;boundaryByExternal.set(id,stubId);visibleNodeMap.set(stubId,{...source,workstream_id:stubId,display_prefix:'外部',display_name:source?.display_name||id,is_boundary_stub:true,boundary_external_id:id,program_id:null,phase_id:null,series_id:null,series_order:null,task_code:null})});let edges=rawEdges.map(edge=>{const fromIn=allowedNodes.has(edge.displayFrom),toIn=allowedNodes.has(edge.displayTo);if(fromIn&&toIn)return edge;if(!state.externalContext&&baseFullIds.has(edge.displayFrom)&&boundaryByExternal.has(edge.displayTo))return{...edge,displayTo:boundaryByExternal.get(edge.displayTo)};if(!state.externalContext&&baseFullIds.has(edge.displayTo)&&boundaryByExternal.has(edge.displayFrom))return{...edge,displayFrom:boundaryByExternal.get(edge.displayFrom)};return null}).filter(Boolean);const seriesEndpointIds=new Set(edges.filter(edge=>edge.relation_type==='series-display'&&allowedNodes.has(edge.displayFrom)&&allowedNodes.has(edge.displayTo)).flatMap(edge=>[edge.displayFrom,edge.displayTo])),programEndpointIds=new Set((data.nodes||[]).filter(node=>node.program_id&&node.phase_id&&allowedNodes.has(node.workstream_id)).map(node=>node.workstream_id)),historyCandidateIds=new Set((data.history_candidate_ids||[]).filter(id=>allowedNodes.has(id))),foldEligibleIds=new Set((data.nodes||[]).filter(node=>allowedNodes.has(node.workstream_id)&&historyFoldEligible(node)).map(node=>node.workstream_id));
  const relationEndpointIds=new Set();edges.forEach(edge=>{relationEndpointIds.add(edge.displayFrom);relationEndpointIds.add(edge.displayTo)});const endpointIds=new Set(relationEndpointIds);if(state.lens==='succession'&&state.scope==='program-w')programEndpointIds.forEach(id=>endpointIds.add(id));if(state.lens==='succession')activeTipIds.forEach(id=>{if(allowedNodes.has(id))endpointIds.add(id)});
  const incoming=new Map(),outgoing=new Map();endpointIds.forEach(id=>{incoming.set(id,new Set());outgoing.set(id,new Set())});
  edges.forEach(edge=>{outgoing.get(edge.displayFrom).add(edge.displayTo);incoming.get(edge.displayTo).add(edge.displayFrom)});
  const visibleFactIds=new Set(),chains=[],claimedHistory=new Set(),protectedDefaultIds=new Set();
  if(state.lens==='conflict'){endpointIds.forEach(id=>visibleFactIds.add(id))}else{
    const lensTips=state.lens==='succession'?[...activeTipIds].filter(id=>endpointIds.has(id)):[];
    const tips=sorted(new Set([...endpointIds].filter(id=>!historyCandidateIds.has(id)&&!(outgoing.get(id)||new Set()).size).concat(lensTips)));
    tips.forEach(tip=>{const chainId=`chain:${tip}`,direct=new Set(incoming.get(tip)||[]),siblings=new Set();direct.forEach(id=>(outgoing.get(id)||[]).forEach(value=>siblings.add(value)));const defaults=new Set([tip,...direct,...siblings]);defaults.forEach(id=>protectedDefaultIds.add(id));const older=orderedAncestors(tip,incoming).filter(id=>!defaults.has(id)&&!claimedHistory.has(id)&&!historyCandidateIds.has(id)&&!seriesEndpointIds.has(id)&&!programEndpointIds.has(id)&&foldEligibleIds.has(id));older.forEach(id=>claimedHistory.add(id));defaults.forEach(id=>visibleFactIds.add(id));if(state.expandedChains.has(chainId))older.forEach(id=>visibleFactIds.add(id));chains.push({chainId,tipId:tip,directIds:sorted(direct),historyIds:older,expanded:state.expandedChains.has(chainId)})});seriesEndpointIds.forEach(id=>visibleFactIds.add(id));if(state.lens==='succession'&&state.scope==='program-w')programEndpointIds.forEach(id=>visibleFactIds.add(id));
  }
  relationEndpointIds.forEach(id=>{if(!claimedHistory.has(id))visibleFactIds.add(id)});
  const nodes=sorted(visibleFactIds).map(id=>{const item=visibleNodeMap.get(id);return item?{...item}:null}).filter(Boolean),hiddenOwner=new Map();
  chains.forEach(chain=>{if(!chain.expanded||!chain.historyIds.length)return;const toggleNode=nodes.find(node=>node.workstream_id===chain.historyIds[0]);if(toggleNode){toggleNode.collapse_chain_id=chain.chainId;toggleNode.expanded_history_count=chain.historyIds.length}});
  chains.forEach(chain=>{if(chain.expanded||!chain.historyIds.length)return;const clusterId=`history:${chain.tipId}`;chain.historyIds.forEach(id=>hiddenOwner.set(id,clusterId));nodes.push({workstream_id:clusterId,display_prefix:`+${chain.historyIds.length}`,display_name:'折叠的上游链',status:'inactive',runtime_condition:'collapsed',lifecycle_phase:'historical',evidence_freshness:'historical',scope_status:'historical',primary_subsystem_id:'历史任务',source_links:[],is_cluster:true,chain_id:chain.chainId,cluster_ids:chain.historyIds,cluster_first_id:chain.historyIds[0],cluster_last_id:chain.historyIds[chain.historyIds.length-1],cluster_tip_id:chain.tipId})});
  if(graphMode==='compact'){
    const visibleIds=new Set(nodes.filter(node=>!node.is_cluster).map(node=>node.workstream_id)),protectedIds=new Set([...visibleIds].filter(id=>!historyCandidateIds.has(id))),attentionEdges=[...(data.edges||[]),...(data.conflicts||[])];
    attentionEdges.forEach(edge=>{if(edge.relation_type==='depends_on'||edge.relation_type==='conflict-fact'||edge.lifecycle==='proposed'||['unknown','unconfirmed','proposed'].includes(edge.certainty)){protectedIds.add(edge.source_workstream_id);protectedIds.add(edge.target_workstream_id)}});
    if(state.selection?.kind==='node')protectedIds.add(state.selection.item.workstream_id);
    if(state.selection?.kind==='edge'){protectedIds.add(state.selection.item.source_workstream_id);protectedIds.add(state.selection.item.target_workstream_id)}
    const allAdjacency=new Map([...visibleIds].map(id=>[id,new Set()]));[...(data.edges||[]),...seriesDisplayEdges()].forEach(edge=>{const left=edge.source_workstream_id,right=edge.target_workstream_id;if(allAdjacency.has(left)&&allAdjacency.has(right)){allAdjacency.get(left).add(right);allAdjacency.get(right).add(left)}});
    [...protectedIds].forEach(id=>(allAdjacency.get(id)||[]).forEach(neighbor=>protectedIds.add(neighbor)));
    const eligible=new Set([...visibleIds].filter(id=>historyCandidateIds.has(id)&&!protectedIds.has(id))),eligibleAdjacency=new Map([...eligible].map(id=>[id,new Set()]));
    edges.forEach(edge=>{if(eligible.has(edge.displayFrom)&&eligible.has(edge.displayTo)){eligibleAdjacency.get(edge.displayFrom).add(edge.displayTo);eligibleAdjacency.get(edge.displayTo).add(edge.displayFrom)}});
    const visitedEligible=new Set(),folds=[];sorted(eligible).forEach(seed=>{if(visitedEligible.has(seed))return;const members=[],pending=[seed];visitedEligible.add(seed);while(pending.length){const id=pending.shift();members.push(id);sorted(eligibleAdjacency.get(id)||[]).forEach(next=>{if(!visitedEligible.has(next)){visitedEligible.add(next);pending.push(next)}})}if(members.length>1)folds.push(sorted(members))});
    folds.forEach(members=>{const foldId=`compact:${members.join('~')}`;if(state.expandedFolds.has(foldId)){const anchorNode=nodes.find(node=>node.workstream_id===members[0]);if(anchorNode){anchorNode.collapse_fold_id=foldId;anchorNode.expanded_history_count=members.length}return}const memberSet=new Set(members),relationEdges=edges.filter(edge=>memberSet.has(edge.displayFrom)||memberSet.has(edge.displayTo)),externalEdges=relationEdges.filter(edge=>memberSet.has(edge.displayFrom)!==memberSet.has(edge.displayTo)),types=sorted(new Set(relationEdges.map(edge=>edge.relation_type))),memberNodes=members.map(id=>visibleNodeMap.get(id)),seriesIds=sorted(new Set(memberNodes.map(node=>node?.series_id).filter(Boolean))),phaseIds=sorted(new Set(memberNodes.map(node=>node?.phase_id).filter(Boolean))),label=seriesIds.length===1?`${seriesIds[0]} · 历史子图`:phaseIds.length===1?`${programGroups.get(phaseIds[0])?.display_label||phaseIds[0]} · 历史子图`:'历史关系子图',summaryId=`history-fold:${folds.indexOf(members)+1}:${members[0]}`;members.forEach(id=>hiddenOwner.set(id,summaryId));for(let index=nodes.length-1;index>=0;index--)if(memberSet.has(nodes[index].workstream_id))nodes.splice(index,1);nodes.push({workstream_id:summaryId,display_prefix:`+${members.length}`,display_name:label,status:'inactive',runtime_condition:'collapsed',lifecycle_phase:'historical',evidence_freshness:'historical',scope_status:'historical',primary_subsystem_id:'历史任务',affected_subsystem_ids:[],source_links:[],is_cluster:true,is_history_fold_summary:true,fold_id:foldId,cluster_ids:members,cluster_first_id:members[0],cluster_last_id:members[members.length-1],cluster_tip_id:'',fold_relation_count:relationEdges.length,fold_external_count:externalEdges.length,fold_relation_types:types})});
  }
  const nodeIds=new Set(nodes.map(node=>node.workstream_id)),displayEdges=[],seenEdgeIds=new Set();
  edges.forEach(edge=>{const from=hiddenOwner.get(edge.displayFrom)||edge.displayFrom,to=hiddenOwner.get(edge.displayTo)||edge.displayTo;if(!nodeIds.has(from)||!nodeIds.has(to)||seenEdgeIds.has(edge.displayEdgeId))return;seenEdgeIds.add(edge.displayEdgeId);displayEdges.push({...edge,displayFrom:from,displayTo:to,collapsedHistoryEdge:from!==edge.displayFrom||to!==edge.displayTo,collapsedInternalEdge:from===to})});
  const relatedNodeIds=new Set(displayEdges.filter(edge=>!edge.collapsedInternalEdge).flatMap(edge=>[edge.displayFrom,edge.displayTo]));nodes.forEach(node=>{node.history_identity_only=!node.is_cluster&&historyCandidateIds.has(node.workstream_id)&&!relatedNodeIds.has(node.workstream_id)});
  nodes.filter(node=>node.is_cluster).forEach(cluster=>{const members=new Set(cluster.cluster_ids);cluster.cluster_status=cluster.is_history_fold_summary?`${cluster.fold_relation_count} 条底层关系 · ${cluster.fold_external_count} 条入口/出口`:'已完成并具备当前证据';cluster.external_in_count=edges.filter(edge=>!members.has(edge.displayFrom)&&members.has(edge.displayTo)).length;cluster.external_out_count=edges.filter(edge=>members.has(edge.displayFrom)&&!members.has(edge.displayTo)).length});
  const rankEdges=partialOrderEdges(),rank=new Map([...nodeIds].map(id=>[id,0]));
  const undirected=new Map([...nodeIds].map(id=>[id,new Set()]));displayEdges.forEach(edge=>{undirected.get(edge.displayFrom).add(edge.displayTo);undirected.get(edge.displayTo).add(edge.displayFrom)});const components=[],assigned=new Set();sorted(nodeIds).forEach(seed=>{if(assigned.has(seed))return;const members=[],pending=[seed];assigned.add(seed);while(pending.length){const id=pending.shift();members.push(id);sorted(undirected.get(id)).forEach(next=>{if(!assigned.has(next)){assigned.add(next);pending.push(next)}})}components.push(sorted(members))});
  const mobile=window.matchMedia('(max-width:640px)').matches,nodeTop=84,nodeLeft=144,positions=new Map();
  const PACK_WIDTH=1180,BLOCK_GAP=56,ROUTE_GUTTER=72,blockById=new Map(),componentLayouts=[];
  let maxNodeBottom=nodeTop,maxNodeRight=nodeLeft;
  components.sort((a,b)=>{const phaseOrder=values=>Math.min(...values.map(id=>programGroups.get(nodeMap.get(id)?.phase_id)?.order??999));return phaseOrder(a)-phaseOrder(b)||(!a.some(id=>activeTipIds.has(id)))-(!b.some(id=>activeTipIds.has(id)))||a[0].localeCompare(b[0])}).forEach((members,componentIndex)=>{
    const memberIds=new Set(members),localRank=localRanks(memberIds,rankEdges,hiddenOwner),byRank=new Map(),orderIndex=new Map();
    members.forEach(id=>{const value=localRank.get(id)||0;rank.set(id,value);if(!byRank.has(value))byRank.set(value,[]);byRank.get(value).push(id)});
    sorted(byRank.keys()).forEach(rankValue=>{byRank.get(rankValue).sort((a,b)=>{const median=id=>{const previous=sorted(undirected.get(id)||[]).filter(next=>orderIndex.has(next)&&(localRank.get(next)||0)<rankValue).map(next=>orderIndex.get(next));return previous.length?previous.reduce((sum,value)=>sum+value,0)/previous.length:Infinity};return median(a)-median(b)||(programGroups.get(nodeMap.get(a)?.phase_id)?.order??999)-(programGroups.get(nodeMap.get(b)?.phase_id)?.order??999)||a.localeCompare(b)});byRank.get(rankValue).forEach((id,index)=>orderIndex.set(id,index))});
    const slots=new Map(),columnRows=new Map();let maxColumn=0,maxRow=0;
    sorted(byRank.keys()).forEach(rankValue=>{byRank.get(rankValue).sort((a,b)=>{const af=sorted(undirected.get(a)||[]).some(next=>(localRank.get(next)||0)>rankValue),bf=sorted(undirected.get(b)||[]).some(next=>(localRank.get(next)||0)>rankValue);return Number(af)-Number(bf)||(orderIndex.get(a)||0)-(orderIndex.get(b)||0)||a.localeCompare(b)}).forEach(id=>{const hasForward=sorted(undirected.get(id)||[]).some(next=>(localRank.get(next)||0)>rankValue),column=rankValue*2+(hasForward?1:0),row=columnRows.get(column)||0;columnRows.set(column,row+1);slots.set(id,[column,row]);maxColumn=Math.max(maxColumn,column);maxRow=Math.max(maxRow,row)})});
    const headerLabel='',headerHeight=0,headerGap=0,nodeRects=new Map();
    members.forEach(id=>{const [column,row]=slots.get(id);nodeRects.set(id,{x:ROUTE_GUTTER+column*(NODE_W+RANK_GAP),y:ROUTE_GUTTER+headerGap+row*(NODE_H+ROW_GAP),w:NODE_W,h:NODE_H,rank:localRank.get(id)||0,row,component:componentIndex})});
    const contentRight=Math.max(...[...nodeRects.values()].map(box=>box.x+box.w)),contentBottom=Math.max(...[...nodeRects.values()].map(box=>box.y+box.h)),headerRects=headerLabel?[{x:ROUTE_GUTTER,y:ROUTE_GUTTER-38,w:Math.min(360,contentRight-ROUTE_GUTTER),h:headerHeight,label:headerLabel,component:componentIndex}]:[];
    componentLayouts.push({component:componentIndex,memberIds:sorted(memberIds),nodeRects,headerRects,routeGutters:{left:ROUTE_GUTTER,right:ROUTE_GUTTER,top:ROUTE_GUTTER,bottom:ROUTE_GUTTER},occupiedBounds:{x:0,y:0,w:contentRight+ROUTE_GUTTER,h:contentBottom+ROUTE_GUTTER}})
  });
  const componentBounds=[],componentHeaders=[],placementTranslations=[];let packX=nodeLeft,packY=nodeTop,packRowHeight=0;
  componentLayouts.forEach(layout=>{const occupied=layout.occupiedBounds;if(packX>nodeLeft&&packX+occupied.w>nodeLeft+PACK_WIDTH){packX=nodeLeft;packY+=packRowHeight+BLOCK_GAP;packRowHeight=0}const dx=packX-occupied.x,dy=packY-occupied.y;placementTranslations.push({component:layout.component,dx,dy});layout.nodeRects.forEach((box,id)=>{const finalBox={...box,x:box.x+dx,y:box.y+dy};positions.set(id,finalBox);blockById.set(id,`component:${layout.component}`);maxNodeRight=Math.max(maxNodeRight,finalBox.x+finalBox.w);maxNodeBottom=Math.max(maxNodeBottom,finalBox.y+finalBox.h)});layout.headerRects.forEach(header=>componentHeaders.push({...header,x:header.x+dx,y:header.y+dy}));componentBounds.push({component:layout.component,x:packX,y:packY,w:occupied.w,h:occupied.h,memberIds:layout.memberIds});packX+=occupied.w+BLOCK_GAP;packRowHeight=Math.max(packRowHeight,occupied.h)});
  const programBands=[];declareBundles(displayEdges,blockById);const bundleCandidates=new Map();displayEdges.filter(edge=>edge.routeBundleId).forEach(edge=>{if(!bundleCandidates.has(edge.routeBundleId))bundleCandidates.set(edge.routeBundleId,[]);bundleCandidates.get(edge.routeBundleId).push(edge)});bundleCandidates.forEach(values=>{const endpoint=values[0].routeBundleEndpoint,commonId=endpoint==='source'?values[0].displayFrom:values[0].displayTo,otherIds=new Set(values.map(edge=>endpoint==='source'?edge.displayTo:edge.displayFrom)),common=positions.get(commonId),centerX=common.x+common.w/2,boundaries=[...otherIds].map(id=>positions.get(id)),aligned=boundaries.filter(box=>box.x<=centerX&&centerX<=box.x+box.w).length>1;if(aligned)values.forEach(edge=>{delete edge.routeBundleId;delete edge.routeBundleEndpoint})});
  const outgoingPorts=new Map(),incomingPorts=new Map();displayEdges.forEach(edge=>{if(!outgoingPorts.has(edge.displayFrom))outgoingPorts.set(edge.displayFrom,[]);if(!incomingPorts.has(edge.displayTo))incomingPorts.set(edge.displayTo,[]);outgoingPorts.get(edge.displayFrom).push(edge.displayEdgeId);incomingPorts.get(edge.displayTo).push(edge.displayEdgeId)});[...outgoingPorts.values(),...incomingPorts.values()].forEach(values=>values.sort());
  const routeBundles=[],bundleGeometry=new Map(),bundleMembers=new Map();displayEdges.filter(edge=>edge.routeBundleId).forEach(edge=>{if(!bundleMembers.has(edge.routeBundleId))bundleMembers.set(edge.routeBundleId,[]);bundleMembers.get(edge.routeBundleId).push(edge)});sorted(bundleMembers.keys()).forEach(bundleId=>{const values=bundleMembers.get(bundleId),endpoint=values[0].routeBundleEndpoint,commonId=endpoint==='source'?values[0].displayFrom:values[0].displayTo,common=positions.get(commonId),others=values.map(edge=>positions.get(endpoint==='source'?edge.displayTo:edge.displayFrom)),above=others.filter(box=>box.y+box.h<=common.y).length>=others.length/2;let trunk,trunkY;if(endpoint==='source'){const start=[common.x+common.w/2,above?common.y:common.y+common.h],boundaries=above?others.filter(box=>box.y+box.h<=common.y).map(box=>box.y+box.h):others.filter(box=>box.y>=common.y+common.h).map(box=>box.y),nearest=boundaries.length?(above?Math.max(...boundaries):Math.min(...boundaries)):(above?common.y-40:common.y+common.h+40);trunkY=Math.round((start[1]+nearest)/2);trunk=[start,[start[0],trunkY]]}else{const end=[common.x+common.w/2,above?common.y+common.h:common.y];trunkY=above?Math.max(...others.map(box=>box.y+box.h))+24:Math.min(...others.map(box=>box.y))-24;trunk=[[end[0],trunkY],end]}const bundle={bundleId,endpoint,edgeIds:sorted(values.map(edge=>edge.displayEdgeId)),trunkPoints:trunk};routeBundles.push(bundle);bundleGeometry.set(bundleId,{...bundle,trunkY})});
  routeBundles.forEach(bundle=>{const start=bundle.trunkPoints[0],end=bundle.trunkPoints[1],delta=end[1]-start[1];if(Math.abs(delta)>64)end[1]=start[1]+Math.sign(delta)*64;const geometry=bundleGeometry.get(bundle.bundleId);if(geometry)geometry.trunkY=end[1]});
  const pairSlots=new Map();
  const routes=displayEdges.map((edge,index)=>{
    const from=positions.get(edge.displayFrom),to=positions.get(edge.displayTo),outgoing=outgoingPorts.get(edge.displayFrom),incoming=incomingPorts.get(edge.displayTo),kind=edge.relation_type,sameRow=from.y===to.y,pairKey=[edge.displayFrom,edge.displayTo].sort().join('|'),pairSlot=pairSlots.get(pairKey)||0;
    pairSlots.set(pairKey,pairSlot+1);
    const leftToRight=to.x>=from.x+from.w,rightToLeft=from.x>=to.x+to.w,corridorLeft=leftToRight?from.x+from.w:to.x+to.w,corridorRight=leftToRight?to.x:from.x;
    const horizontalBlocked=[...positions.entries()].some(([nodeId,box])=>![edge.displayFrom,edge.displayTo].includes(nodeId)&&box.x<corridorRight&&box.x+box.w>corridorLeft);
    let points,sourcePort,targetPort;
    const bundle=edge.routeBundleId?bundleGeometry.get(edge.routeBundleId):null;
    if(bundle&&bundle.endpoint==='source'){
      const start=bundle.trunkPoints[0],targetSlot=incoming.indexOf(edge.displayEdgeId),endX=to.x+Math.round((targetSlot+1)*to.w/(incoming.length+1)),end=[endX,to.y<bundle.trunkY?to.y+to.h:to.y];points=[start,bundle.trunkPoints[1],[endX,bundle.trunkY],end];sourcePort='bundle-trunk';targetPort=to.y<bundle.trunkY?'south':'north';
    }else if(bundle&&bundle.endpoint==='target'){
      const sourceSlot=outgoing.indexOf(edge.displayEdgeId),startX=from.x+Math.round((sourceSlot+1)*from.w/(outgoing.length+1)),start=[startX,from.y>bundle.trunkY?from.y:from.y+from.h];points=[start,[startX,bundle.trunkY],bundle.trunkPoints[0],bundle.trunkPoints[1]];sourcePort=from.y>bundle.trunkY?'north':'south';targetPort='bundle-trunk';
    }else if(kind==='series-display'&&sameRow){
      points=[[from.x+from.w,from.y+from.h/2],[to.x,to.y+to.h/2]];sourcePort='east-center';targetPort='west-center';
    }else if(sameRow&&(leftToRight||rightToLeft)&&!horizontalBlocked){
      points=leftToRight?[[from.x+from.w,from.y+from.h/2],[to.x,to.y+to.h/2]]:[[from.x,from.y+from.h/2],[to.x+to.w,to.y+to.h/2]];sourcePort=leftToRight?'east-center':'west-center';targetPort=leftToRight?'west-center':'east-center';
    }else if(sameRow){
      const sx=from.x+Math.round((outgoing.indexOf(edge.displayEdgeId)+1)*from.w/(outgoing.length+1)),tx=to.x+Math.round((incoming.indexOf(edge.displayEdgeId)+1)*to.w/(incoming.length+1)),trackY=Math.max(from.y+from.h,to.y+to.h)+24+pairSlot*18;
      points=[[sx,from.y+from.h],[sx,trackY],[tx,trackY],[tx,to.y+to.h]];sourcePort='south';targetPort='south';
    }else if((leftToRight||rightToLeft)&&!horizontalBlocked){
      const targetLower=to.y>from.y,sourceFan=Math.round((outgoing.indexOf(edge.displayEdgeId)-(outgoing.length-1)/2)*4),targetFan=Math.round((incoming.indexOf(edge.displayEdgeId)-(incoming.length-1)/2)*4),sy=targetLower?from.y+from.h-10+sourceFan:from.y+10+sourceFan,ty=targetLower?to.y+10+targetFan:to.y+to.h-10+targetFan,start=[leftToRight?from.x+from.w:from.x,sy],end=[leftToRight?to.x:to.x+to.w,ty],corridorFan=Math.round((outgoing.indexOf(edge.displayEdgeId)-(outgoing.length-1)/2)*16);
      let midX=Math.round((start[0]+end[0])/2)+corridorFan+pairSlot*18;midX=Math.max(Math.min(start[0],end[0])+16,Math.min(Math.max(start[0],end[0])-16,midX));
      points=[start,[midX,start[1]],[midX,end[1]],end];sourcePort=leftToRight?'east':'west';targetPort=leftToRight?'west':'east';
    }else{
      const down=to.y>=from.y+from.h,gapTop=down?from.y+from.h:to.y+to.h,gapBottom=down?to.y:from.y,routeLeft=Math.min(from.x,to.x),routeRight=Math.max(from.x+from.w,to.x+to.w),verticalBlockers=[...positions.entries()].filter(([nodeId,box])=>![edge.displayFrom,edge.displayTo].includes(nodeId)&&box.y<gapBottom&&box.y+box.h>gapTop&&box.x<routeRight&&box.x+box.w>routeLeft).map(([,box])=>box);
      if(verticalBlockers.length){const corridorX=Math.max(from.x+from.w,to.x+to.w,...verticalBlockers.map(box=>box.x+box.w))+16+pairSlot*18,sourceFan=Math.round((outgoing.indexOf(edge.displayEdgeId)-(outgoing.length-1)/2)*8),targetFan=Math.round((incoming.indexOf(edge.displayEdgeId)-(incoming.length-1)/2)*8),start=[from.x+from.w,(down?from.y+from.h-16:from.y+16)+sourceFan],end=[to.x+to.w,(down?to.y+16:to.y+to.h-16)+targetFan];points=[start,[corridorX,start[1]],[corridorX,end[1]],end];sourcePort='east';targetPort='east'}else{const sx=from.x+Math.round((outgoing.indexOf(edge.displayEdgeId)+1)*from.w/(outgoing.length+1)),tx=to.x+Math.round((incoming.indexOf(edge.displayEdgeId)+1)*to.w/(incoming.length+1)),start=[sx,down?from.y+from.h:from.y],end=[tx,down?to.y:to.y+to.h],midY=Math.round((start[1]+end[1])/2)+pairSlot*18;points=[start,[start[0],midY],[end[0],midY],end];sourcePort=down?'south':'north';targetPort=down?'north':'south'}
    }
    points=points.filter((point,i)=>!i||point[0]!==points[i-1][0]||point[1]!==points[i-1][1]);
    const segments=points.slice(1).map((point,i)=>({a:points[i],b:point,length:Math.hypot(point[0]-points[i][0],point[1]-points[i][1])})),longest=[...segments].sort((a,b)=>b.length-a.length)[0],labelPoint=[Math.round((longest.a[0]+longest.b[0])/2),Math.round((longest.a[1]+longest.b[1])/2)],hasLabel=['series-display','comparison-suggestion','conflict-fact'].includes(kind)||['proposed','stale'].includes(edge.lifecycle)||['unknown','unconfirmed'].includes(edge.certainty),routeLength=segments.reduce((total,segment)=>total+segment.length,0),directDistance=Math.hypot(points.at(-1)[0]-points[0][0],points.at(-1)[1]-points[0][1]);
    return{edge,points,branchPoints:bundle?(bundle.endpoint==='source'?points.slice(1):points.slice(0,-1)):points,labelPoint,hasLabel,sourcePort,targetPort,bendCount:Math.max(0,points.length-2),routeLength,directDistance,stretch:routeLength/Math.max(1,directDistance),shortestSegment:Math.min(...segments.map(segment=>segment.length)),lineEncoding:kind==='series-display'?'series':kind==='comparison-suggestion'?'comparison':state.lens==='dependency'?'dashed':state.lens==='conflict'?'compound':'solid',routeBundleId:edge.routeBundleId||null,component:from.component};
  });
  const labelRects=placeRouteLabels(routes,positions,componentBounds,componentHeaders);
  const maxRank=positions.size?Math.max(...[...positions.values()].map(position=>position.rank)):-1,lanes=[];for(let value=0;value<=maxRank;value++){const values=nodes.filter(node=>positions.get(node.workstream_id).rank===value);let title=values.some(node=>node.is_cluster)?'更早历史':values.some(node=>activeTipIds.has(node.workstream_id))?'当前任务':value===maxRank?'当前／后续任务':'直接前置／依赖';lanes.push({rank:value,title})}const seriesLanes=[];
  const roles=new Map();nodes.forEach(node=>{let role='history';if(node.is_boundary_stub)role='boundary';else if(node.is_cluster)role='cluster';else if(state.lens==='conflict')role='conflict';else if([node.status,node.runtime_condition,node.evidence_freshness,node.scope_status].includes('unknown'))role='unknown';else if(activeTipIds.has(node.workstream_id)||chains.some(chain=>chain.tipId===node.workstream_id))role='current';else if(chains.some(chain=>chain.directIds.includes(node.workstream_id)))role='direct';roles.set(node.workstream_id,role)});
  const canvasRight=Math.max(maxNodeRight,...componentBounds.map(box=>box.x+box.w)),canvasBottom=Math.max(maxNodeBottom,...componentBounds.map(box=>box.y+box.h));
  const model={mobile,nodes,edges:displayEdges,chains,positions,routes,lanes,seriesLanes,programBands,componentHeaders,componentBounds,placementTranslations,labelRects,routeBundles,roles,width:nodeIds.size?canvasRight+44:1,height:nodeIds.size?canvasBottom+54:1,visibleFactIds:sorted(visibleFactIds)};model.geometryPostconditions=verifyGeometry(model);page.dataset.wgGeometryPassed=String(model.geometryPostconditions.passed);page.dataset.wgGeometryCounts=JSON.stringify(model.geometryPostconditions.counts);return model;
}
function pathData(points){let path=`M${points[0][0]} ${points[0][1]}`;for(let index=1;index<points.length;index++){const previous=points[index-1],point=points[index];path+=point[1]===previous[1]?` H${point[0]}`:` V${point[1]}`}return path}
function shortId(value,max=29){return value.length>max?value.slice(0,max-1)+'…':value}
function nameLines(value){const chars=[...String(value||'任务记录')];return[chars.slice(0,15).join(''),chars.slice(15,30).join('')].filter(Boolean)}
function roleCopy(role,node){if(role==='boundary')return'外部模块关联';if(node?.history_identity_only)return'仅找回身份';if(node&&!node.is_cluster&&node.plain_status)return node.plain_status;return{current:'正在进行',direct:'直接前置',history:'历史任务',unknown:'关系证据不足',conflict:'冲突事实',cluster:'历史分组'}[role]||'任务'}
function markerId(edge){if(edge.relation_type==='series-display')return'wg-arrow-series';if(edge.relation_type==='comparison-suggestion')return'wg-arrow-comparison';return state.lens==='dependency'?'wg-arrow-dependency':state.lens==='conflict'?'wg-arrow-conflict':'wg-arrow-succession'}
function applySelectionFocus(){svg.querySelectorAll('g.is-muted,g.is-selected,.wg-node').forEach(item=>item.classList.remove('is-muted','is-selected','is-endpoint','is-node-selected'));if(!state.selection)return;if(state.selection.kind==='node'){const nodeId=state.selection.item.workstream_id;svg.querySelectorAll('.wg-node').forEach(item=>{const selected=item.dataset.wgNodeId===nodeId;item.classList.toggle('is-node-selected',selected);item.classList.toggle('is-muted',!selected)});svg.querySelectorAll('g[data-wg-edge-group]').forEach(item=>item.classList.add('is-muted'));return}const selectedIds=new Set(state.selection.kind==='bundle'?state.selection.item.edgeIds:[state.selection.item.displayEdgeId||state.selection.item.relation_id||state.selection.item.id]),endpoints=new Set(state.selection.kind==='bundle'?state.selection.item.edges.flatMap(edge=>[edge.displayFrom,edge.displayTo]):[state.selection.item.displayFrom,state.selection.item.displayTo]);svg.querySelectorAll('.wg-edge').forEach(path=>{const item=path.parentElement,selected=selectedIds.has(path.dataset.wgEdgeId);item.classList.toggle('is-muted',!selected);item.classList.toggle('is-selected',selected)});svg.querySelectorAll('.wg-node').forEach(item=>{const endpoint=endpoints.has(item.dataset.wgNodeId);item.classList.toggle('is-endpoint',endpoint);item.classList.toggle('is-muted',!endpoint)})}
function openInspector(kind,item){state.selection={kind,item};renderInspector();inspector.hidden=false;inspector.setAttribute('aria-hidden','false');applySelectionFocus();live.textContent=(kind==='node'?'已选择任务 ':'已选择关系 ')+(item.workstream_id||item.relation_id||item.id)}
function closeInspector(){inspector.hidden=true;inspector.setAttribute('aria-hidden','true');state.selection=null;applySelectionFocus();live.textContent=''}
function renderInspector(){
  inspectorBody.replaceChildren();
  if(!state.selection)return;
  const {kind,item}=state.selection;
  if(kind==='bundle'){
    inspectorBody.append(el('h3',null,`接续束线 · ${item.edgeIds.length} 条关系`),el('p','wg-inspector-summary','共享主干只用于同语义关系的屏幕呈现；每条关系的证据与身份仍然独立。'));
    item.edges.forEach(edge=>{const button=el('button','wg-comparison');button.type='button';button.append(el('b',null,`${edge.displayFrom} → ${edge.displayTo}`),el('small',null,edge.displayEdgeId));keyboardActivate(button,()=>openInspector('edge',edge));inspectorBody.append(button)});
    return;
  }
  const heading=el('h3',null,kind==='node'?(item.display_prefix||'任务')+' · '+(item.display_name||'任务记录'):relationLabel(item));
  const relationSummary=item.relation_type==='series-display'?'只反映显式系列顺序，不构成依赖、接续或 gate。':item.relation_type==='comparison-suggestion'?'只读比较建议，不是冲突事实，也不会阻塞任务。':'记录方向与屏幕阅读方向分开显示';
  const summary=el('p','wg-inspector-summary',kind==='node'?(item.plain_status||'关系证据不足'):relationSummary);
  inspectorBody.append(heading,summary);
  const conflict=item.conflict_evidence||{};
  const axes=kind==='node'?[['普通状态',item.plain_status],['状态代码',item.plain_status_code],['所属程序',item.program_id||'未登记'],['阶段',item.phase_id||'未登记'],['任务系列',item.series_id||'未登记'],['系列任务码',item.task_code||'未登记'],['生命周期',item.lifecycle_phase],['运行状态',item.runtime_condition],['证据状态',item.evidence_freshness],['范围状态',item.scope_status],['项目模块',item.primary_subsystem_id],['可见范围',item.visibility],['观测来源',item.observability]]:[['关系类型',item.relation_type],['版本数',item.relation_revision_count||1],['束线',item.routeBundleId],['系列',item.series_id],['阻塞阶段',item.required_for],['生命周期',item.lifecycle],['确定性',item.certainty],['屏幕方向',item.displayFrom+' → '+item.displayTo],['Core source',item.source_workstream_id],['Core target',item.target_workstream_id],['冲突位置',Array.isArray(conflict.location)?conflict.location.join('；'):conflict.location],['冲突影响',conflict.impact],['冲突来源',conflict.source],['处置',item.disposition||'edge']];
  axes.forEach(([key,value])=>{const row=el('div','wg-axis');row.append(el('span',null,key),el('b',null,value==null?'Unknown':String(value)));inspectorBody.append(row)});
  const reasons=item.reason_codes||item.evidence_reason_codes||[];
  if(reasons.length){const list=el('ul','wg-reasons');reasons.forEach(reason=>list.append(el('li',null,reasonLabels[reason]||reason)));inspectorBody.append(list)}
  const history=item.relation_history||[];
  if(kind!=='node'&&history.length){
    const section=el('section','wg-relation-history'),title=el('h4',null,`历史 revision · ${history.length}`);
    section.append(title);
    history.forEach(revision=>{const card=el('div','wg-relation-revision'),status=[revision.lifecycle||'Unknown',revision.certainty||'Unknown',revision.evidence_status||'Unknown'].join(' · '),reasonText=(revision.evidence_reason_codes||[]).map(reason=>reasonLabels[reason]||reason).join('；')||'无附加原因';card.append(el('b',null,revision.relation_id||'Unknown relation id'),el('small',null,status),el('small',null,reasonText));section.append(card)});
    inspectorBody.append(section);
  }
  const links=el('div','wg-evidence');
  (item.source_links||[]).forEach(link=>{const node=link.href?el('a',null,link.kind+' · '+link.ref):el('span',null,link.kind+' · '+link.ref);if(link.href){node.href=link.href;node.rel='noopener'}links.append(node)});
  if(!(item.source_links||[]).length)links.append(el('span',null,'没有可跳转的来源链接；证据状态见上方技术字段。'));
  inspectorBody.append(links);
}
function toggleChain(chainId,anchor=null){const before=anchor?.getBoundingClientRect(),model=window.__orreryWorkstreamGraph?.model,cluster=model?.nodes.find(node=>node.chain_id===chainId&&node.is_cluster),restoreId=cluster?.cluster_first_id;state.expandedChains.has(chainId)?state.expandedChains.delete(chainId):state.expandedChains.add(chainId);render();if(before&&restoreId){const restored=svg.querySelector(`[data-wg-node-id="${CSS.escape(restoreId)}"]`);if(restored){const after=restored.getBoundingClientRect();viewport.scrollBy({left:after.left-before.left,top:after.top-before.top,behavior:'instant'})}}}
function preserveViewport(){state.viewportAnchor={x:viewport.scrollWidth>viewport.clientWidth?viewport.scrollLeft/(viewport.scrollWidth-viewport.clientWidth):0,y:viewport.scrollHeight>viewport.clientHeight?viewport.scrollTop/(viewport.scrollHeight-viewport.clientHeight):0}}
function toggleFold(foldId){preserveViewport();state.expandedFolds.has(foldId)?state.expandedFolds.delete(foldId):state.expandedFolds.add(foldId);render()}
function setGraphMode(mode){if(!['full','compact'].includes(mode)||state.graphMode===mode)return;preserveViewport();state.graphMode=mode;if(mode==='full')state.expandedFolds.clear();page.querySelectorAll('[data-wg-mode]').forEach(button=>button.setAttribute('aria-pressed',String(button.dataset.wgMode===mode)));render();live.textContent=mode==='full'?'已显示完整关系':'已折叠深层历史关系'}
function humanLabel(id){const item=nodeMap.get(id);return item?`${item.display_prefix||'任务'} · ${item.display_name||'任务记录'}`:String(id)}
function renderNode(node,position,role){const clusterSummary=node.is_history_fold_summary?`${node.cluster_ids.length} 个历史任务，保留 ${node.fold_relation_count} 条底层关系和 ${node.fold_external_count} 条入口/出口，点击原地展开`:node.is_cluster?`${node.cluster_ids.length} 个有关系证据的上游任务，点击展开`:'',membership=node.phase_id?`W › ${programGroups.get(node.phase_id)?.display_label||node.phase_id}`:node.series_id?`${node.series_id} 系列`:'组织分类未登记',statusCopy=node.plain_status||'关系证据不足',fullLabel=node.is_cluster?`${roleCopy(role,node)}，${node.display_name}，${clusterSummary}`:`${roleCopy(role,node)} ${node.task_code||node.display_prefix||"任务"} ${node.display_name||"任务记录"}，${node.workstream_id}，${membership}，${statusCopy}`,clipId=`wg-node-clip-${String(node.workstream_id).replace(/[^A-Za-z0-9_.-]/g,"-")}`,group=se('g',{class:`wg-node ${role}${node.is_history_fold_summary?' history-fold':''}`,'data-wg-node-id':node.workstream_id,tabindex:0,role:'button','aria-label':fullLabel});group.append(se('rect',{x:position.x,y:position.y,width:position.w,height:position.h,rx:9}));const title=se('title');title.textContent=fullLabel;const clip=se('clipPath',{id:clipId});clip.append(se('rect',{x:position.x+2,y:position.y+2,width:Math.max(0,position.w-4),height:Math.max(0,position.h-4),rx:7}));const content=se('g',{'clip-path':`url(#${clipId})`}),roleText=se('text',{x:position.x+14,y:position.y+20,class:'wg-node-role'});roleText.textContent=node.is_history_fold_summary?'折叠历史':roleCopy(role,node);const prefix=se('text',{x:position.x+14,y:position.y+42,class:'wg-node-prefix'});prefix.textContent=node.task_code||node.display_prefix||'任务';content.append(roleText,prefix);nameLines(node.display_name).forEach((line,index)=>{const text=se('text',{x:position.x+64,y:position.y+42+index*17,class:'wg-node-name'});text.textContent=line;content.append(text)});const machine=se('text',{x:position.x+14,y:position.y+70,class:'wg-node-id'});machine.textContent=node.is_history_fold_summary?shortId(`${node.cluster_ids.length} 项 · ${node.fold_relation_count} 关系 · ${node.fold_external_count} 出入口`,36):node.is_cluster?shortId(`${node.cluster_ids.length} 项 · 上游链`,36):shortId(node.workstream_id);const meta=se('text',{x:position.x+14,y:position.y+87,class:'wg-node-meta'});meta.textContent=node.is_history_fold_summary?`关系类型：${(node.fold_relation_types||[]).map(type=>type==='derived_from'?'接续':type==='depends_on'?'依赖':type).join('／')||'无'}`:node.is_cluster?'点击展开这条关系链':`${membership} · ${statusCopy}`;content.append(machine,meta);if(node.collapse_chain_id||node.collapse_fold_id){const foldControl=Boolean(node.collapse_fold_id),control=se('g',{class:'wg-chain-collapse',tabindex:0,role:'button','aria-label':`${foldControl?'重新折叠历史子图':'收起历史'}，${node.expanded_history_count} 个任务`}),controlRect=se('rect',{x:position.x+position.w-82,y:position.y+8,width:70,height:23,rx:5}),controlText=se('text',{x:position.x+position.w-47,y:position.y+23,'text-anchor':'middle'});controlText.textContent=foldControl?'重新折叠':'收起历史';const collapse=event=>{event.preventDefault();event.stopPropagation();foldControl?toggleFold(node.collapse_fold_id):toggleChain(node.collapse_chain_id,control)};control.addEventListener('click',collapse);control.addEventListener('keydown',event=>{if(event.key==='Enter'||event.key===' ')collapse(event)});control.append(controlRect,controlText);content.append(control)}group.append(title,clip,content);const action=node.is_history_fold_summary?()=>toggleFold(node.fold_id):node.is_cluster?()=>toggleChain(node.chain_id,group):()=>openInspector('node',node);keyboardActivate(group,action);return group}
function renderedNodeOverlapPairs(){const boxes=[...svg.querySelectorAll('.wg-node')].map(node=>{const rect=node.querySelector(':scope > rect');return{id:node.dataset.wgNodeId,x:Number(rect?.getAttribute('x')),y:Number(rect?.getAttribute('y')),w:Number(rect?.getAttribute('width')),h:Number(rect?.getAttribute('height'))}}),pairs=[];for(let left=0;left<boxes.length;left++)for(let right=left+1;right<boxes.length;right++){const a=boxes[left],b=boxes[right],width=Math.min(a.x+a.w,b.x+b.w)-Math.max(a.x,b.x),height=Math.min(a.y+a.h,b.y+b.h)-Math.max(a.y,b.y);if(width>0&&height>0)pairs.push({source:a.id,target:b.id,width,height,area:width*height})}return pairs}
function organizationalClassificationCounts(nodes){const visible=nodes.filter(node=>!node.is_cluster),missingSeries=visible.filter(node=>!node.series_id).length,missingProgram=visible.filter(node=>!node.program_id||!node.phase_id).length,missingBoth=visible.filter(node=>!node.series_id&&(!node.program_id||!node.phase_id)).length;return{visibleNodes:visible.length,missingSeries,missingProgram,missingBoth}}
function emptyMessage(){if(data.status!=='ready')return'当前没有完整证据，因此未生成不完整关系图。';if(state.subsystem!=='all')return`${subsystem.options[subsystem.selectedIndex]?.text||state.subsystem} 当前没有匹配的任务证据`;if(state.lens==='dependency')return'当前没有已登记的依赖关系';if(state.lens==='conflict')return'当前没有已确认的任务冲突';return'当前没有已登记的接续关系'}
function renderEmpty(empty,model){empty.replaceChildren();if(model.nodes.length){empty.hidden=true;return}const title=el('b',null,emptyMessage()),detail=el('p',null,data.status!=='ready'?'只有完整、只读且哈希绑定的证据才能进入关系图。':state.lens==='dependency'?'接续关系不等于依赖关系。本页不会把接续边重复画成依赖，也不会用没有连线的任务填满画布。':state.lens==='conflict'?'只有已登记的冲突对会出现在这里；没有证据时不推断冲突。':'当前筛选条件没有留下可显示的接续链。'),count=el('small',null,state.lens==='dependency'?'0 条依赖边 · 0 个孤立节点':state.lens==='conflict'?'0 组冲突对':'0 条可见接续边');empty.append(title,detail,count);empty.hidden=false}
function renderLedger(model){ledger.replaceChildren();ledger.dataset.factIds=model.visibleFactIds.join('|');if(!model.nodes.length&&!model.edges.length){ledger.append(el('div','wg-ledger-empty',emptyMessage()));return}const labels=new Map(model.nodes.map(node=>[node.workstream_id,node.is_cluster?node.display_name:`${node.display_prefix||'任务'} · ${node.display_name||'任务记录'}`]));model.lanes.forEach(lane=>{ledger.append(el('div','wg-ledger-rank',`第 ${lane.rank+1} 层 · ${lane.title}`));model.nodes.filter(node=>model.positions.get(node.workstream_id).rank===lane.rank).forEach(node=>{const role=model.roles.get(node.workstream_id),button=el('button',`wg-ledger-item ${role}`);button.type='button';button.style.setProperty('--wg-indent',`${Math.min(lane.rank,3)*12}px`);button.append(el('b',null,`${node.is_history_fold_summary?'折叠历史':roleCopy(role,node)} · ${node.task_code||node.display_prefix||'任务'} · ${node.display_name||'任务记录'}`),el('small',null,node.is_history_fold_summary?`${node.cluster_ids.length} 项 · ${node.fold_relation_count} 条底层关系 · ${node.fold_external_count} 条入口/出口；点击原地展开。`:node.is_cluster?`${node.cluster_ids.length} 项有关系证据的上游任务；点击展开。`:`${node.series_id?node.series_id+' 系列 · ':''}${shortId(node.workstream_id,44)}`));keyboardActivate(button,node.is_history_fold_summary?()=>toggleFold(node.fold_id):node.is_cluster?()=>toggleChain(node.chain_id,button):()=>openInspector('node',node));ledger.append(button);if(node.collapse_chain_id||node.collapse_fold_id){const isFold=Boolean(node.collapse_fold_id),collapse=el('button','wg-ledger-collapse',`${isFold?'重新折叠历史子图':'收起历史'} · ${node.expanded_history_count} 项`);collapse.type='button';keyboardActivate(collapse,isFold?()=>toggleFold(node.collapse_fold_id):()=>toggleChain(node.collapse_chain_id));ledger.append(collapse)}});model.edges.filter(edge=>model.positions.get(edge.displayTo).rank===lane.rank).forEach(edge=>{const button=el('button',`wg-ledger-edge ${state.lens}`);button.type='button';button.title=`${edge.displayFrom} → ${edge.displayTo}`;button.append(el('b',null,relationLabel(edge)),el('small',null,`从 ${labels.get(edge.displayFrom)||'关系证据不足'} → 到 ${labels.get(edge.displayTo)||'关系证据不足'}`));keyboardActivate(button,()=>openInspector('edge',edge));ledger.append(button)})})}
const reasonLabels={'stale-or-unknown-endpoint':'端点状态待刷新或证据不足','sibling-successors':'同一前序产生了多个后续任务，需要比较','execution-dependency-does-not-suppress':'存在执行依赖，不能仅按接续关系排除','succession-stale-or-unconfirmed':'接续关系尚未确认或证据已过期','parent-post-fork-or-unknown-commits':'父任务在分叉后仍有提交，提交归属需要比较','independent-or-unproven-pair':'当前没有关系证据证明二者相关','completed-takeover-predecessor-not-closed-superseded':'责任接管已完成，但前序关闭／替代证据不足'};
function renderComparisons(){comparisons.replaceChildren();const items=data.comparison_suggestions||[];comparisonCount.textContent=String(items.length);if(!items.length){comparisons.append(el('div','wg-ledger-empty','当前没有需要比较或刷新证据的任务对。'));return}comparisonDisplayEdges().forEach(item=>{const card=el('button','wg-comparison'),reasons=(item.reason_codes||[]).map(reason=>reasonLabels[reason]||reason);card.type='button';card.append(el('b',null,`${item.source_workstream_id} ↔ ${item.target_workstream_id}`),el('small',null,reasons.join('；')));keyboardActivate(card,()=>openInspector('edge',item));comparisons.append(card)})}
function paintModel(model){
  const total=useTotalSuccessionCompounds();
  svg.replaceChildren();
  svg.dataset.factIds=model.visibleFactIds.join('|');
  svg.setAttribute('viewBox',`0 0 ${model.width} ${model.height}`);
  svg.setAttribute('width',String(model.width*state.zoom));
  svg.setAttribute('height',String(model.height*state.zoom));
  zoomReadout.textContent=`${Math.round(state.zoom*100)}%`;
  const defs=se('defs');
  [['succession','#6ee7dd'],['dependency','#f4c96b'],['conflict','#ff786c'],['series','#94a3b7'],['comparison','#f4c96b']].forEach(([id,color])=>{const marker=se('marker',{id:`wg-arrow-${id}`,viewBox:'0 0 12 12',refX:10.5,refY:6,markerWidth:10,markerHeight:10,markerUnits:'userSpaceOnUse',orient:'auto'});marker.append(se('path',{d:'M2 2L10.5 6L2 10Z',fill:color}));defs.append(marker)});
  svg.append(defs);
  model.programBands.forEach(program=>{svg.append(se('rect',{x:program.x,y:program.y,width:program.w,height:program.h,rx:14,class:'wg-program-band'}));const title=se('text',{x:program.x+12,y:program.y+17,class:'wg-program-title'});title.textContent=program.label;svg.append(title);program.phases.forEach(phase=>{svg.append(se('rect',{x:phase.x,y:phase.y,width:phase.w,height:phase.h,rx:10,class:'wg-phase-band'}));const phaseTitle=se('text',{x:phase.x+10,y:phase.y+17,class:'wg-phase-title'});phaseTitle.textContent=phase.label;svg.append(phaseTitle)})});
  model.seriesLanes.forEach(lane=>{const title=se('text',{x:lane.x+10,y:lane.y+15,class:'wg-series-title'});title.textContent=`${lane.seriesId} 系列`;if(total){const guide=se('line',{x1:lane.x+82,y1:lane.y+12,x2:Math.max(lane.x+82,lane.x+lane.w-10),y2:lane.y+12,class:'wg-series-guide'});svg.append(guide,title)}else{const band=se('rect',{x:lane.x,y:lane.y,width:lane.w,height:lane.h,rx:12,class:'wg-series-band'});svg.append(band,title)}});
  model.routeBundles.forEach(bundle=>{const group=se('g',{'data-wg-bundle-id':bundle.bundleId}),trunk=se('path',{d:pathData(bundle.trunkPoints),class:`wg-bundle-trunk lens-${state.lens}`}),hit=se('path',{d:pathData(bundle.trunkPoints),class:'wg-bundle-hit',tabindex:0,role:'button','aria-label':`共享接续主干，${bundle.edgeIds.length} 条关系`}),edges=bundle.edgeIds.map(id=>model.edges.find(edge=>edge.displayEdgeId===id)).filter(Boolean);keyboardActivate(hit,()=>openInspector('bundle',{...bundle,edges}));group.append(trunk,hit);svg.append(group)});
  model.routes.forEach(route=>{const edge=route.edge,fullLabel=relationLabel(edge),canvasLabel=relationLabel(edge,true),lifecycle=edge.lifecycle==='proposed'?'proposed':edge.lifecycle==='stale'?'stale':'confirmed',edgeKind=edge.relation_type==='series-display'?'series-display':edge.relation_type==='comparison-suggestion'?'comparison-suggestion':'semantic',group=se('g',{'data-wg-edge-group':edge.displayEdgeId}),drawPoints=route.branchPoints||route.points,path=se('path',{d:pathData(drawPoints),class:`wg-edge lens-${state.lens} ${edgeKind} ${lifecycle} ${edge.certainty==='confirmed'?'':'uncertain'}`,'data-wg-edge-id':edge.displayEdgeId,'marker-end':`url(#${markerId(edge)})`}),hit=se('path',{d:pathData(drawPoints),class:'wg-edge-hit',tabindex:0,role:'button','aria-label':`${fullLabel}，从 ${edge.displayFrom} 到 ${edge.displayTo}`}),tooltip=se('title');tooltip.textContent=`${fullLabel} · ${edge.displayFrom} → ${edge.displayTo}`;keyboardActivate(hit,()=>openInspector('edge',edge));group.append(tooltip,path,hit);if(!total&&route.hasLabel&&!route.routeBundleId){const text=se('text',{x:route.labelPoint[0],y:route.labelPoint[1]-8,class:`wg-edge-label ${edgeKind}`,'text-anchor':'middle'});text.textContent=canvasLabel;group.append(text)}svg.append(group)});
  model.nodes.forEach(node=>svg.append(renderNode(node,model.positions.get(node.workstream_id),model.roles.get(node.workstream_id))));
  const overlapPairs=renderedNodeOverlapPairs(),priorCounts=model.geometryPostconditions?.counts||{};
  model.geometryPostconditions={...(model.geometryPostconditions||{}),passed:overlapPairs.length===0,counts:{...priorCounts,finalNodeOverlapPairs:overlapPairs.length},finalNodeOverlapPairs:overlapPairs};
  page.dataset.wgGeometryPassed=String(model.geometryPostconditions.passed);
  page.dataset.wgGeometryCounts=JSON.stringify(model.geometryPostconditions.counts);
  page.dataset.wgFullClassificationCounts=JSON.stringify(model.fullClassificationCounts);
  classificationStatus.textContent=`完整图 ${model.fullClassificationCounts.visibleNodes} 节点：未登记系列 ${model.fullClassificationCounts.missingSeries}、未登记程序/阶段 ${model.fullClassificationCounts.missingProgram}、两者皆未登记 ${model.fullClassificationCounts.missingBoth}；生命周期与组织分类分轴。`;
  if(overlapPairs.length){const overlapError=new Error(`ELK 最终节点矩形重叠 ${overlapPairs.length} 对：${overlapPairs.map(pair=>`${pair.source} ↔ ${pair.target}`).join('；')}`);overlapError.orreryGeometryFailure=true;throw overlapError}
  renderLedger(model);
  renderComparisons();
  renderEmpty(page.querySelector('[data-wg-empty]'),model);
  comparisonToggle.disabled=state.lens==='conflict';
  comparisonToggle.setAttribute('aria-pressed',String(state.comparisons));
  comparisonToggle.textContent=state.comparisons?'隐藏比较建议':'显示比较建议';
  applySelectionFocus();
  page.dataset.wgLayoutReady='true';
  window.__orreryWorkstreamGraph={model,state};
  if(state.viewportAnchor){const anchor=state.viewportAnchor;state.viewportAnchor=null;requestAnimationFrame(()=>{viewport.scrollLeft=anchor.x*Math.max(0,viewport.scrollWidth-viewport.clientWidth);viewport.scrollTop=anchor.y*Math.max(0,viewport.scrollHeight-viewport.clientHeight)})}
}
function useTotalSuccessionCompounds(){return state.scope==='all'&&state.lens==='succession'&&state.subsystem==='all'&&state.runtime==='all'&&!state.externalContext&&!state.affectedContext}
function elkNode(node,direction='RIGHT',ports=null){const vertical=direction==='DOWN';if(!ports?.length)return{id:node.workstream_id,width:NODE_W,height:NODE_H,layoutOptions:{'elk.portConstraints':'FIXED_SIDE'},ports:[{id:`${node.workstream_id}::in`,width:1,height:1,layoutOptions:{'elk.port.side':vertical?'NORTH':'WEST'}},{id:`${node.workstream_id}::out`,width:1,height:1,layoutOptions:{'elk.port.side':vertical?'SOUTH':'EAST'}}]};const bySide=new Map();ports.forEach(port=>{if(!bySide.has(port.side))bySide.set(port.side,[]);bySide.get(port.side).push(port)});const resolved=[];bySide.forEach((values,side)=>{const spine=values.filter(port=>port.spine),others=values.filter(port=>!port.spine).sort((left,right)=>left.id.localeCompare(right.id));spine.forEach(port=>resolved.push({...port,offset:NODE_H/2}));others.forEach((port,index)=>{const slots=others.length+(spine.length?1:0),slot=index+(spine.length&&index>=Math.floor(others.length/2)?2:1),offset=spine.length?slot*NODE_H/(slots+1):(index+1)*NODE_H/(others.length+1);resolved.push({...port,offset})})});return{id:node.workstream_id,width:NODE_W,height:NODE_H,layoutOptions:{'elk.portConstraints':'FIXED_POS'},ports:resolved.map(port=>({id:port.id,x:port.side==='WEST'?-0.5:NODE_W-0.5,y:port.offset-0.5,width:1,height:1,layoutOptions:{'elk.port.side':port.side}}))}}
function edgeNeedsLabel(edge){if(useTotalSuccessionCompounds()&&edge.relation_type==='series-display')return false;return['series-display','comparison-suggestion','conflict-fact'].includes(edge.relation_type)||['proposed','stale'].includes(edge.lifecycle)||['unknown','unconfirmed'].includes(edge.certainty)}
function elkEdge(edge,sourcePort=`${edge.displayFrom}::out`,targetPort=`${edge.displayTo}::in`){const series=edge.relation_type==='series-display';return{id:edge.displayEdgeId,sources:[sourcePort],targets:[targetPort],labels:edgeNeedsLabel(edge)?[{id:`${edge.displayEdgeId}::label`,text:relationLabel(edge,true),width:92,height:18}]:[],layoutOptions:{'elk.layered.priority.direction':series?'20':'7','elk.layered.priority.straightness':series?'100':'1'}}}
function relationComponents(nodes,edges){const adjacency=new Map(nodes.map(node=>[node.workstream_id,new Set()]));edges.forEach(edge=>{adjacency.get(edge.displayFrom)?.add(edge.displayTo);adjacency.get(edge.displayTo)?.add(edge.displayFrom)});const remaining=new Set(adjacency.keys()),components=[];while(remaining.size){const first=sorted(remaining)[0],queue=[first],ids=[];remaining.delete(first);while(queue.length){const current=queue.shift();ids.push(current);sorted(adjacency.get(current)||[]).forEach(next=>{if(!remaining.has(next))return;remaining.delete(next);queue.push(next)})}components.push(sorted(ids))}return components.sort((left,right)=>{const leftSeries=left.some(id=>nodeMap.get(id)?.series_id),rightSeries=right.some(id=>nodeMap.get(id)?.series_id);return Number(rightSeries)-Number(leftSeries)||left[0].localeCompare(right[0])})}
function elkCompoundOptions(direction,hasEdges=true){return{'elk.algorithm':hasEdges?'layered':'box','elk.direction':direction,'elk.edgeRouting':'ORTHOGONAL','elk.hierarchyHandling':'INCLUDE_CHILDREN','elk.separateConnectedComponents':'false','elk.spacing.nodeNode':'46','elk.layered.spacing.nodeNodeBetweenLayers':direction==='RIGHT'?'74':'90','elk.layered.spacing.edgeNodeBetweenLayers':'36','elk.layered.spacing.edgeEdgeBetweenLayers':'18','elk.layered.nodePlacement.strategy':'BRANDES_KOEPF','elk.layered.nodePlacement.bk.fixedAlignment':'BALANCED','elk.layered.nodePlacement.bk.edgeStraightening':'IMPROVE_STRAIGHTNESS','elk.layered.nodePlacement.favorStraightEdges':'true','elk.layered.crossingMinimization.strategy':'LAYER_SWEEP','elk.layered.considerModelOrder.strategy':'NODES_AND_EDGES','elk.layered.mergeEdges':'false','elk.padding':'[top=24,left=24,bottom=24,right=24]'}}
function elkTotalSuccessionGraph(model,edges){
  const nodesById=new Map(model.nodes.map(node=>[node.workstream_id,node])),seriesCounts=new Map(),components=relationComponents(model.nodes,edges),portSpecs=new Map(model.nodes.map(node=>[node.workstream_id,[]])),edgePorts=new Map(),componentByNode=new Map();
  model.nodes.forEach(node=>{if(node.series_id)seriesCounts.set(node.series_id,(seriesCounts.get(node.series_id)||0)+1)});
  components.forEach((ids,index)=>ids.forEach(id=>componentByNode.set(id,index)));
  edges.forEach(edge=>{const sourcePort=`${edge.displayFrom}::${edge.displayEdgeId}::out`,targetPort=`${edge.displayTo}::${edge.displayEdgeId}::in`,spine=edge.relation_type==='series-display';portSpecs.get(edge.displayFrom)?.push({id:sourcePort,side:'EAST',spine});portSpecs.get(edge.displayTo)?.push({id:targetPort,side:'WEST',spine});edgePorts.set(edge.displayEdgeId,{sourcePort,targetPort})});
  const componentChildren=components.map((ids,index)=>{
    const idSet=new Set(ids),componentNodes=ids.map(id=>nodesById.get(id)).filter(Boolean),componentEdges=edges.filter(edge=>idSet.has(edge.displayFrom)&&idSet.has(edge.displayTo)),seriesGroups=new Map();
    componentNodes.forEach(node=>{if(!node.series_id||(seriesCounts.get(node.series_id)||0)<2)return;if(!seriesGroups.has(node.series_id))seriesGroups.set(node.series_id,[]);seriesGroups.get(node.series_id).push(node)});
    const containedSeriesEdges=new Set(),nestedSeriesNodeIds=new Set(),seriesOrder=new Map([['authority-a',0],['ci',1],['unified-u',2]]),seriesChildren=sorted(seriesGroups.keys()).sort((left,right)=>(seriesOrder.get(left)??99)-(seriesOrder.get(right)??99)||left.localeCompare(right)).map(seriesId=>{
      const seriesNodes=seriesGroups.get(seriesId).sort((left,right)=>(left.series_order??Number.MAX_SAFE_INTEGER)-(right.series_order??Number.MAX_SAFE_INTEGER)||left.workstream_id.localeCompare(right.workstream_id)),seriesIds=new Set(seriesNodes.map(node=>node.workstream_id)),seriesEdges=componentEdges.filter(edge=>edge.relation_type==='series-display'&&seriesIds.has(edge.displayFrom)&&seriesIds.has(edge.displayTo));
      const hasCrossGroupEdge=componentEdges.some(edge=>seriesIds.has(edge.displayFrom)!==seriesIds.has(edge.displayTo));
      if(hasCrossGroupEdge)return null;
      seriesNodes.forEach(node=>nestedSeriesNodeIds.add(node.workstream_id));
      seriesEdges.forEach(edge=>containedSeriesEdges.add(edge.displayEdgeId));
      return{id:`layout-series:${seriesId}`,layoutOptions:elkCompoundOptions('RIGHT',seriesEdges.length>0),children:seriesNodes.map(node=>elkNode(node,'RIGHT',portSpecs.get(node.workstream_id))),edges:seriesEdges.map(edge=>{const ports=edgePorts.get(edge.displayEdgeId);return elkEdge(edge,ports.sourcePort,ports.targetPort)})};
    }).filter(Boolean);
    const ungrouped=componentNodes.filter(node=>!nestedSeriesNodeIds.has(node.workstream_id)).sort((left,right)=>left.workstream_id.localeCompare(right.workstream_id)).map(node=>elkNode(node,'RIGHT',portSpecs.get(node.workstream_id))),outerEdges=componentEdges.filter(edge=>!containedSeriesEdges.has(edge.displayEdgeId)).sort((left,right)=>left.displayEdgeId.localeCompare(right.displayEdgeId));
    return{id:`layout-component:${index}:${ids[0]}`,layoutOptions:elkCompoundOptions('RIGHT',outerEdges.length>0),children:[...ungrouped,...seriesChildren],edges:outerEdges.map(edge=>{const ports=edgePorts.get(edge.displayEdgeId);return elkEdge(edge,ports.sourcePort,ports.targetPort)})};
  });
  return{id:'orrery-workstream-graph',layoutOptions:{'elk.algorithm':'box','elk.direction':'RIGHT','elk.spacing.nodeNode':'28','elk.padding':'[top=8,left=8,bottom=8,right=8]'},children:componentChildren,edges:[]};
}
function elkGraph(model,direction='RIGHT'){const nodeIds=new Set(model.nodes.map(node=>node.workstream_id)),edges=model.edges.filter(edge=>nodeIds.has(edge.displayFrom)&&nodeIds.has(edge.displayTo)&&!edge.collapsedInternalEdge);if(useTotalSuccessionCompounds())return elkTotalSuccessionGraph(model,edges);return{id:'orrery-workstream-graph',layoutOptions:{'elk.algorithm':edges.length?'layered':'box','elk.direction':direction,'elk.edgeRouting':'ORTHOGONAL','elk.separateConnectedComponents':'true','elk.spacing.componentComponent':'72','elk.spacing.nodeNode':'52','elk.layered.spacing.nodeNodeBetweenLayers':'112','elk.layered.spacing.edgeNodeBetweenLayers':'46','elk.layered.spacing.edgeEdgeBetweenLayers':'22','elk.layered.nodePlacement.strategy':'NETWORK_SIMPLEX','elk.layered.crossingMinimization.strategy':'LAYER_SWEEP','elk.layered.considerModelOrder.strategy':'NODES_AND_EDGES','elk.padding':'[top=28,left=28,bottom=28,right=28]'},children:model.nodes.map(node=>elkNode(node,direction)),edges:edges.map(edge=>elkEdge(edge))}}
function sectionPoints(section,offsetX=0,offsetY=0){return[section.startPoint,...(section.bendPoints||[]),section.endPoint].map(point=>[Math.round(point.x+offsetX),Math.round(point.y+offsetY)])}
function fallbackPoints(from,to){const start=[from.x+from.w,from.y+from.h/2],end=[to.x,to.y+to.h/2],mid=Math.round((start[0]+end[0])/2);return[start,[mid,start[1]],[mid,end[1]],end]}
function elkResultModel(semantic,layout){
  const positions=new Map(),semanticIds=new Set(semantic.nodes.map(node=>node.workstream_id)),routed=new Map(),seriesLanes=[],componentBounds=[];
  function visit(graph,offsetX=0,offsetY=0,topComponent=null){
    (graph.edges||[]).forEach(edge=>routed.set(edge.id,{edge,offsetX,offsetY}));
    (graph.children||[]).forEach(child=>{const x=offsetX+(child.x||0),y=offsetY+(child.y||0),component=topComponent||child.id;if(semanticIds.has(child.id))positions.set(child.id,{x:Math.round(x),y:Math.round(y),w:Math.round(child.width||NODE_W),h:Math.round(child.height||NODE_H),rank:0,row:0,component});if(child.id.startsWith('layout-series:'))seriesLanes.push({seriesId:child.id.slice('layout-series:'.length),x:Math.round(x-8),y:Math.round(y-22),w:Math.round((child.width||1)+16),h:Math.round((child.height||1)+30)});if(offsetX===0&&offsetY===0&&child.id.startsWith('layout-component:'))componentBounds.push({component:child.id,x:Math.round(x),y:Math.round(y),w:Math.round(child.width||1),h:Math.round(child.height||1),memberIds:[]});visit(child,x,y,component)})
  }
  visit(layout);
  componentBounds.forEach(bound=>{bound.memberIds=[...positions.entries()].filter(([,box])=>box.component===bound.component).map(([id])=>id)});
  const xRanks=sorted(new Set([...positions.values()].map(item=>item.x)));
  positions.forEach(item=>{item.rank=xRanks.indexOf(item.x)});
  const routes=[];
  semantic.edges.forEach(edge=>{if(edge.collapsedInternalEdge)return;const from=positions.get(edge.displayFrom),to=positions.get(edge.displayTo);if(!from||!to)return;const routedItem=routed.get(edge.displayEdgeId),elkEdge=routedItem?.edge,section=elkEdge?.sections?.[0],offsetX=routedItem?.offsetX||0,offsetY=routedItem?.offsetY||0,points=section?sectionPoints(section,offsetX,offsetY):fallbackPoints(from,to),label=elkEdge?.labels?.[0],segments=points.slice(1).map((point,index)=>Math.hypot(point[0]-points[index][0],point[1]-points[index][1])),routeLength=segments.reduce((sum,value)=>sum+value,0),directDistance=Math.hypot(points.at(-1)[0]-points[0][0],points.at(-1)[1]-points[0][1]);routes.push({edge,points,branchPoints:points,labelPoint:label?[Math.round(label.x+label.width/2+offsetX),Math.round(label.y+label.height+offsetY)]:[Math.round((points[0][0]+points.at(-1)[0])/2),Math.round((points[0][1]+points.at(-1)[1])/2)],hasLabel:edgeNeedsLabel(edge),sourcePort:'elk',targetPort:'elk',bendCount:Math.max(0,points.length-2),routeLength,directDistance,stretch:routeLength/Math.max(1,directDistance),shortestSegment:segments.length?Math.min(...segments):0,lineEncoding:edge.relation_type==='series-display'?'series':edge.relation_type==='comparison-suggestion'?'comparison':state.lens==='dependency'?'dashed':state.lens==='conflict'?'compound':'solid',routeBundleId:null,component:from.component})});
  const maxRank=positions.size?Math.max(...[...positions.values()].map(item=>item.rank)):-1,lanes=[];
  for(let rank=0;rank<=maxRank;rank++)lanes.push({rank,title:rank===0?'较早任务':rank===maxRank?'当前／后续任务':'关系中段'});
  return{...semantic,positions,routes,lanes,seriesLanes,programBands:[],componentHeaders:[],componentBounds:componentBounds.length?componentBounds:[{component:0,x:0,y:0,w:Math.ceil(layout.width||1),h:Math.ceil(layout.height||1),memberIds:semantic.nodes.map(node=>node.workstream_id)}],placementTranslations:componentBounds.map(bound=>({component:bound.component,dx:bound.x,dy:bound.y})),labelRects:[],routeBundles:[],width:Math.max(1,Math.ceil(layout.width||1)),height:Math.max(1,Math.ceil(layout.height||1)),geometryPostconditions:{passed:null,counts:{}}}
}
async function elkLayoutModel(semantic){if(!semantic.nodes.length)return{...semantic,width:1,height:1,routes:[],programBands:[],seriesLanes:[],routeBundles:[]};if(state.scope==='program-w')return elkPhaseMultiples(semantic);return elkResultModel(semantic,await new ELK().layout(elkGraph(semantic,'RIGHT')))}
async function elkPhaseMultiples(semantic){const phases=['workstream-w5','workstream-w6','workstream-w7'],phaseOf=new Map(semantic.nodes.filter(node=>phases.includes(node.phase_id)).map(node=>[node.workstream_id,node.phase_id]));semantic.edges.forEach(edge=>{const from=phaseOf.get(edge.displayFrom),to=phaseOf.get(edge.displayTo);if(from&&!to)phaseOf.set(edge.displayTo,from);if(to&&!from)phaseOf.set(edge.displayFrom,to)});const panels=await Promise.all(phases.map(async phaseId=>{const nodes=semantic.nodes.filter(node=>phaseOf.get(node.workstream_id)===phaseId),ids=new Set(nodes.map(node=>node.workstream_id)),edges=semantic.edges.filter(edge=>ids.has(edge.displayFrom)&&ids.has(edge.displayTo)),direction=phaseId==='workstream-w7'?'RIGHT':'DOWN',partial={...semantic,nodes,edges};return{phaseId,direction,partial,model:elkResultModel(partial,await new ELK().layout(elkGraph(partial,direction)))}}));const positions=new Map(),routes=[],phaseRects=[];let x=34,maxBottom=0;panels.forEach(({phaseId,model})=>{const offsetX=x+18,offsetY=58;model.positions.forEach((box,id)=>positions.set(id,{...box,x:box.x+offsetX,y:box.y+offsetY,component:phaseId}));model.routes.forEach(route=>routes.push({...route,points:route.points.map(([px,py])=>[px+offsetX,py+offsetY]),branchPoints:route.branchPoints.map(([px,py])=>[px+offsetX,py+offsetY]),labelPoint:[route.labelPoint[0]+offsetX,route.labelPoint[1]+offsetY],component:phaseId}));const width=model.width+36,height=model.height+76,group=programGroups.get(phaseId);phaseRects.push({x,y:20,w:width,h:height,label:group?.display_label||phaseId,phaseId});x+=width+38;maxBottom=Math.max(maxBottom,20+height)});const cross=semantic.edges.filter(edge=>phaseOf.get(edge.displayFrom)!==phaseOf.get(edge.displayTo));cross.forEach(edge=>{const from=positions.get(edge.displayFrom),to=positions.get(edge.displayTo);if(!from||!to)return;const points=fallbackPoints(from,to);routes.push({edge,points,branchPoints:points,labelPoint:[Math.round((points[0][0]+points.at(-1)[0])/2),Math.round((points[0][1]+points.at(-1)[1])/2)],hasLabel:edgeNeedsLabel(edge),sourcePort:'elk-cross-phase',targetPort:'elk-cross-phase',bendCount:2,routeLength:0,directDistance:1,stretch:1,shortestSegment:0,lineEncoding:state.lens==='dependency'?'dashed':'solid',routeBundleId:null,component:'cross-phase'})});const rankXs=sorted(new Set([...positions.values()].map(box=>box.x)));positions.forEach(box=>{box.rank=rankXs.indexOf(box.x)});const maxRank=rankXs.length-1,lanes=rankXs.map((_,rank)=>({rank,title:rank===0?'阶段起点':rank===maxRank?'阶段当前任务':'阶段接续'}));return{...semantic,positions,routes,lanes,seriesLanes:[],programBands:[{x:0,y:0,w:x,h:maxBottom+20,label:'W · Workstream 协作',phases:phaseRects}],componentHeaders:[],componentBounds:phaseRects.map((rect,index)=>({component:rect.phaseId,x:rect.x,y:rect.y,w:rect.w,h:rect.h,memberIds:semantic.nodes.filter(node=>phaseOf.get(node.workstream_id)===rect.phaseId).map(node=>node.workstream_id)})),placementTranslations:phaseRects.map((rect,index)=>({component:rect.phaseId,dx:rect.x,dy:rect.y})),labelRects:[],routeBundles:[],width:Math.max(1,x),height:Math.max(1,maxBottom+40),geometryPostconditions:{passed:null,counts:{}}}}
function semanticInput(model){return{schema_version:1,lens:state.lens,graph_mode:state.graphMode,expanded_folds:sorted(state.expandedFolds),scope:state.scope,module:state.subsystem,runtime:state.runtime,affected_context:state.affectedContext,semantic_context:state.externalContext,node_ids:sorted(model.nodes.map(node=>node.workstream_id)),edge_ids:sorted(model.edges.map(edge=>edge.displayEdgeId))}}
async function inputHash(value){const bytes=new TextEncoder().encode(JSON.stringify(value)),digest=await crypto.subtle.digest('SHA-256',bytes);return[...new Uint8Array(digest)].map(value=>value.toString(16).padStart(2,'0')).join('')}
let renderRevision=0;
async function render(){
  const revision=++renderRevision;
  const hasAcceptedCanvas=page.dataset.wgLayoutReady==='true'&&svg.childNodes.length>0;
  page.classList.toggle('wg-total-succession',useTotalSuccessionCompounds());
  layoutFailure.hidden=true;
  page.dataset.wgRequestedRevision=String(revision);
  if(!hasAcceptedCanvas){page.dataset.wgLayoutReady='false';svg.replaceChildren();svg.setAttribute('viewBox','0 0 1 1');svg.setAttribute('width','1');svg.setAttribute('height','1')}
  page.querySelector('[data-wg-empty]').hidden=true;
  if(state.engine==='elk'){
    page.dataset.wgEngine='elk';
    delete page.dataset.wgInputHash;
    delete page.dataset.wgRequestedHash;
    legacyBanner.hidden=true;
    page.querySelector('[data-wg-empty]').hidden=true;
    layoutLoading.hidden=hasAcceptedCanvas;
    engineStatus.textContent=hasAcceptedCanvas?'ELK 正在准备新视图 · 当前画布继续可读':'ELK 布局中';
    await new Promise(resolve=>requestAnimationFrame(()=>setTimeout(resolve,0)));
    if(revision!==renderRevision)return;
  }else{
    layoutLoading.hidden=true;
    engineStatus.textContent='旧版兼容布局正在计算';
    await new Promise(resolve=>requestAnimationFrame(()=>setTimeout(resolve,0)));
    if(revision!==renderRevision)return;
  }
  const semantic=buildModel(),fullDiagnostic=state.graphMode==='full'?semantic:buildModel('full'),input=semanticInput(semantic);
  semantic.fullClassificationCounts=organizationalClassificationCounts(fullDiagnostic.nodes);
  page.dataset.wgInputRevision=String(input.schema_version);
  page.dataset.wgInputIds=[...input.node_ids,...input.edge_ids].join('|');
  if(state.engine==='legacy'){
    page.dataset.wgEngine='legacy';
    legacyBanner.hidden=false;
    engineStatus.textContent=`旧版兼容布局 · ${input.node_ids.length} 卡片 · ${input.edge_ids.length} 关系`;
    paintModel(semantic);
    return;
  }
  try{
    const previewFailure=['127.0.0.1','localhost'].includes(location.hostname)&&new URLSearchParams(location.search).get('orrery-preview-elk-failure')==='1';
    if(previewFailure)throw new Error('本机预览要求模拟 ELK 失败');
    if(typeof ELK!=='function')throw new Error('本机 ELK 运行时未加载');
    const hash=await inputHash(input);
    if(revision!==renderRevision)return;
    page.dataset.wgRequestedHash=hash;
    const model=await elkLayoutModel(semantic);
    if(revision!==renderRevision||page.dataset.wgRequestedRevision!==String(revision)||page.dataset.wgRequestedHash!==hash)return;
    page.dataset.wgInputHash=hash;
    page.dataset.wgGeometryPassed='pending-preview';
    paintModel(model);
    layoutLoading.hidden=true;
    engineStatus.textContent=`ELK 0.11.0 · ${model.width}×${model.height} · 输入 ${hash.slice(0,10)}`;
  }catch(error){
    if(revision!==renderRevision)return;
    layoutLoading.hidden=true;
    if(error?.orreryGeometryFailure){page.dataset.wgLayoutReady='false'}else if(!hasAcceptedCanvas){svg.replaceChildren();svg.setAttribute('width','1');svg.setAttribute('height','1');renderLedger(semantic);renderComparisons()}else{page.dataset.wgLayoutReady='true'}
    page.querySelector('[data-wg-empty]').hidden=true;
    layoutError.textContent=`${error?.message||error}。同事实清单仍可阅读；不会自动切换旧版布局。`;
    layoutFailure.hidden=false;
    engineStatus.textContent='ELK 布局失败 · 未自动回退';
    window.__orreryWorkstreamGraph={model:semantic,state,error};
  }
}
function applyViewportScale(){const accepted=window.__orreryWorkstreamGraph?.model;if(!accepted||page.dataset.wgLayoutReady!=='true')return false;svg.setAttribute('width',String(accepted.width*state.zoom));svg.setAttribute('height',String(accepted.height*state.zoom));zoomReadout.textContent=`${Math.round(state.zoom*100)}%`;return true}
function zoomAt(value,anchorX,anchorY){const previous=state.zoom,next=Math.max(MIN_ZOOM,Math.min(MAX_ZOOM,value));if(Math.abs(next-previous)<.001||page.dataset.wgLayoutReady!=='true')return;const worldX=(viewport.scrollLeft+anchorX)/previous,worldY=(viewport.scrollTop+anchorY)/previous;state.zoom=next;if(!applyViewportScale()){state.zoom=previous;return}viewport.scrollLeft=worldX*next-anchorX;viewport.scrollTop=worldY*next-anchorY;live.textContent=`关系图缩放到 ${Math.round(next*100)}%`}
function setZoom(value){zoomAt(value,viewport.clientWidth/2,viewport.clientHeight/2)}
function fit(){const model=window.__orreryWorkstreamGraph?.model;if(!model||page.dataset.wgLayoutReady!=='true'||!model.nodes.length)return;state.zoom=Math.max(MIN_ZOOM,Math.min(1.15,Math.max(1,viewport.clientWidth-24)/model.width,Math.max(1,viewport.clientHeight-24)/model.height));applyViewportScale();viewport.scrollTo({left:0,top:0,behavior:'smooth'});live.textContent=`全图已适配到 ${Math.round(state.zoom*100)}%`}
function reset(){state.lens='succession';state.graphMode='full';state.expandedChains.clear();state.expandedFolds.clear();state.scope='all';state.subsystem='all';state.runtime='all';state.zoom=1;state.comparisons=false;state.externalContext=false;state.affectedContext=false;scope.value='all';subsystem.value='all';runtime.value='all';semanticContext.checked=false;affectedContext.checked=false;page.querySelectorAll('[data-wg-lens]').forEach(button=>button.setAttribute('aria-pressed',String(button.dataset.wgLens==='succession')));page.querySelectorAll('[data-wg-mode]').forEach(button=>button.setAttribute('aria-pressed',String(button.dataset.wgMode==='full')));closeInspector();render();viewport.scrollTo({left:0,top:0})}
function options(select,values,labels={}){sorted(new Set(values.filter(Boolean))).forEach(value=>{const option=el('option',null,labels[value]||value);option.value=value;select.append(option)})}
options(subsystem,(data.nodes||[]).flatMap(node=>[node.primary_subsystem_id,...(node.affected_subsystem_ids||[])]),subsystemLabels);if((data.nodes||[]).some(node=>!node.primary_subsystem_id||['unknown','unregistered'].includes(node.primary_subsystem_id))){const option=el('option',null,'未归属');option.value='unassigned';subsystem.append(option)}options(runtime,(data.nodes||[]).map(node=>node.runtime_condition),runtimeLabels);
page.querySelectorAll('[data-wg-lens]').forEach(button=>button.addEventListener('click',()=>{state.lens=button.dataset.wgLens;if(state.lens==='conflict')state.comparisons=false;page.querySelectorAll('[data-wg-lens]').forEach(item=>item.setAttribute('aria-pressed',String(item===button)));closeInspector();render()}));
page.querySelectorAll('[data-wg-mode]').forEach(button=>button.addEventListener('click',()=>setGraphMode(button.dataset.wgMode)));
scope.addEventListener('change',()=>{state.scope=scope.value;closeInspector();render()});subsystem.addEventListener('change',()=>{state.subsystem=subsystem.value;state.externalContext=false;state.affectedContext=false;semanticContext.checked=false;affectedContext.checked=false;closeInspector();render()});runtime.addEventListener('change',()=>{state.runtime=runtime.value;render()});semanticContext.addEventListener('change',()=>{state.externalContext=semanticContext.checked;closeInspector();render()});affectedContext.addEventListener('change',()=>{state.affectedContext=affectedContext.checked;closeInspector();render()});
page.querySelector('[data-wg-zoom-in]').addEventListener('click',()=>setZoom(state.zoom+.1));page.querySelector('[data-wg-zoom-out]').addEventListener('click',()=>setZoom(state.zoom-.1));page.querySelector('[data-wg-fit]').addEventListener('click',fit);page.querySelector('[data-wg-reset]').addEventListener('click',reset);page.querySelector('[data-wg-inspector-close]').addEventListener('click',closeInspector);comparisonToggle.addEventListener('click',()=>{if(state.lens==='conflict')return;state.comparisons=!state.comparisons;closeInspector();render()});
viewport.addEventListener('wheel',event=>{if(!event.ctrlKey)return;event.preventDefault();const rect=viewport.getBoundingClientRect(),anchorX=event.clientX-rect.left,anchorY=event.clientY-rect.top,factor=event.deltaY<0?1.1:.9;zoomAt(state.zoom*factor,anchorX,anchorY)},{passive:false});
let pan=null;viewport.addEventListener('pointerdown',event=>{if(event.target.closest('.wg-node,.wg-edge-hit'))return;pan={x:event.clientX,y:event.clientY,left:viewport.scrollLeft,top:viewport.scrollTop};viewport.classList.add('is-panning');viewport.setPointerCapture(event.pointerId)});viewport.addEventListener('pointermove',event=>{if(!pan)return;viewport.scrollLeft=pan.left-(event.clientX-pan.x);viewport.scrollTop=pan.top-(event.clientY-pan.y)});function endPan(){pan=null;viewport.classList.remove('is-panning')}viewport.addEventListener('pointerup',endPan);viewport.addEventListener('pointercancel',endPan);page.addEventListener('keydown',event=>{if(event.key==='Escape'&&!inspector.hidden)closeInspector()});
engineSelect.addEventListener('change',()=>{state.engine=engineSelect.value;render()});page.querySelector('[data-wg-layout-retry]').addEventListener('click',()=>{state.engine='elk';engineSelect.value='elk';render()});page.querySelector('[data-wg-use-legacy]').addEventListener('click',()=>{state.engine='legacy';engineSelect.value='legacy';render()});render();closeInspector();
})();
"""


__all__ = ["WORKSTREAM_GRAPH_CSS", "WORKSTREAM_GRAPH_JS"]
